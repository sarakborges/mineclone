# HANDOFF — Asteria / Mineclone

Repositório `sarakborges/mineclone`, branch `develop`, Rust + Bevy 0.19.1. **Esta é a fonte canônica persistente.** Históricos íntegros: [pré-.31](https://github.com/sarakborges/mineclone/blob/72b60b5a4e70a9960b3108debdf77d7a8510c4b1/HANDOFF.md), [feedback .31](https://github.com/sarakborges/mineclone/blob/6b8051628aa49c3b01ffca782144b8fc84705365/HANDOFF.md), [estado .32](https://github.com/sarakborges/mineclone/blob/5900057c6e7ec8d53684bf877a47c499ae6d3b5b/HANDOFF.md), [implementação .33](https://github.com/sarakborges/mineclone/blob/a90e42a61d18a8aa4e326b17450f80e5b334de53/HANDOFF.md), [detalhes .34](https://github.com/sarakborges/mineclone/blob/814a59b86fe32c73655a7d27ee4142b3949bf0ae/HANDOFF.md), [estado .35](https://github.com/sarakborges/mineclone/blob/355c0f5eeae8d08738c49f7a895c2d9534714793/HANDOFF.md), [estado .36](https://github.com/sarakborges/mineclone/blob/154e4bfd0d050cb78a7e74c9c210b3aa9676c167/HANDOFF.md) e [estado completo .37](https://github.com/sarakborges/mineclone/blob/1b90522fe0bde385bfa48c09fc93a00de54b0488/HANDOFF.md). Compactação nunca fecha bug.

## Regras obrigatórias

`go`/`continua` = executar, não só planejar. Consultar HEAD, `VERSION` e código real antes de escrever. Trabalhar em `develop` diretamente, sem branches não solicitadas. Todo bloco de código ou dados jogáveis sobe `VERSION`: PATCH correção/refactor compatível, MINOR funcionalidade compatível, MAJOR quebra de contrato. Documento isolado não sobe. `Cargo.toml` versão antiga 0.10.16 é deliberado. **Atualizar este HANDOFF na MESMA SESSÃO** de toda mudança material de código, versão, feedback, roadmap, processo e arquitetura, incluindo SHA, status CI e próximo passo. Erros/warnings reais primeiro, sem silenciar. CI canônico `cargo clippy --all-targets --all-features -- -D warnings` e `cargo check`, não executa unit tests, `fmt` não é gate. Não repetir cargo local/espera de `cargo run`; teste manual somente quando solicitado. Distinguir hipótese, código publicado, CI aprovado e gameplay confirmado. Sem geração de imagens não solicitadas; assets binários do usuário exatos.

## Arquitetura/canon

`ARCHITECTURE.md` autoritativo: owner único por fato, SystemParams por domínio, UI em `src/ui`, targeting `TargetedBlock` único, hot paths change-driven e invariantes reais compartilhados sem abstração vazia. Usar `DeduplicatedQueue<T>`/`VoxelUpdateQueue` e `FrameWorkBudget`, evitar scans globais, alocações, dirty writes supérfluos. Pipeline async seleção visible/preload -> geração -> integração **e seed direta imediata por residência (.38)** -> captura halo 3×3×3 -> mesh -> render/visibilidade -> retenção -> archive/unload. Visible supera preload, preempção devolve não crítico, outputs stale protegidos sem starvation. Geometry/Terrain e Fluid usam meshes, filas e iluminação independentes. `ChunkRemeshTasks::lighting_revisions` ≠ `VoxelWorld::chunk_mesh_revisions`. Mesh inicial aparece rápido, depois reconcilia halo se necessário.

HDR world order0 Skip, viewmodel order1 tonemap final, UI SDR order2; céu por bioma/relógio em `EnvironmentVisualState.sky_color`, `SkyPlugin` aplica ClearColor EXATO após track biome. `fogColor` artístico separado ainda não aplicado; fog terminal precisa casar com sky plano sem mistura fixa. Nuvens world-space, vento independente, seaLevel por dimensão; sol/lua rise/set e órbitas independentes data-driven. Hidrologia endpoints incidentes X/Z/Y compartilhados, margens gerais .12 preservadas, determinismo entre regiões, água natural autoritativa e não enfileirar massa no solver. Held block observer de hotbar/tint, tecla R apenas colocação.

## Estado 16/09/2026

**VERSION = 0.15.38.** Código `src/world/streaming.rs` commit `1d76fae2f7f6ad99fd2d43f318432b9ec519538e`; bump VERSION `74b75bca2fd82ee7af39e8386b37a616945799d0`; HANDOFF commit posterior: consultar HEAD. **CI .37 CONFIRMADO SUCCESS:** push run `35140172992`, job `104942427406`, Clippy e Check success. **CI .38 em andamento ao consultar:** push run `35141651000`, job `104947360899`, Clippy em execução, Check pendente; PR run `35141655419` in progress. Confirmar e corrigir qualquer erro/warning com novo bump/handoff. CI .36 push `35139112147`, job `104938867724`, SUCCESS. Nenhuma CI executa unit tests ou gameplay.

**.38 — corrigida ordem de seed do streaming no código, gameplay NÃO confirmado:** investigação apontou race temporal concreta: `VoxelChunk::empty()` inicializa array de luz em `VoxelLight::DARK`; `VoxelWorld::insert_chunk` disponibilizava chunks recém-gerados ao mundo ainda escuros; antes o seed direto só ocorria em `dispatch_initial_mesh_tasks` DEPOIS da verificação de capacidade para tarefas de mesh. Quando geração se adiantava ao mesh, um chunk pronto podia capturar no seu `ChunkMeshSnapshot` o halo de outro chunk já carregado mas com iluminação toda DARK. Isso pode escurecer faces inteiras e gerar costuras temporárias; NÃO comprova sozinho a causa de todos os chunks pretos. Introduzido helper único `seed_loaded_chunk_lighting`: executa `seed_chunk_direct_lighting`, `enqueue_loaded_fluid_frontier`, marca meshes in-flight vizinhas para reconciliação e enfileira relaxação shell/inteira conforme chunk vazio/não-vazio, uma única vez por residência (`initial_lighting_seeded`), preservando o pipeline e evitando re-seed nos retries. `collect_generated_chunks` agora semeia IMEDIATAMENTE após `insert_chunk`; se resultado já estiver gerado mas arquivado, restaura antes de semear. `dispatch_generation_tasks` semeia imediatamente após `restore_chunk`. `dispatch_initial_mesh_tasks` conserva fallback de seed antes de verificar limite de mesh tasks, mas na rota usual é no-op: o chunk já está semeado. Isso evita capturas de luz DARK de vizinhos em espera no `ready` sem clarear globalmente o shader nem duplicar trackers de revisão. **Risco:** o seed migrou para o orçamento de integração de geração (1 ms; até 8 resultados por frame), podendo diminuir throughput sob deslocamento; medir FPS/frame time, geração/mesh backlog e remesh. Sem novos testes unitários para integração completa, CI apenas Clippy e Check. Para chunks pretos persistentes, seguir auditando `average_shader_light_levels` sem amostras de ar, seed acima da superfície, propagação e publicação assíncrona; defeito #1 permanece aberto.

**.37 — margens regionais corrigidas no código, gameplay não confirmado:** commits `8b2ea3a64f27f32d75bdb1643add9e47b424468e` (`constants.rs`), `56d237034969a44d3c1828ba1798452eed75c1a1` (`spatial.rs`), `e069ac54fd268035b95d2b97c2b8caf571aed408` (`river/path/confluence.rs`), `a4c5333a16ab384b64cc911fb2d5f463bcae7268` (`feature_graph.rs`), `3e10cde00725446769cd30edf0607ae34f218178` (`density.rs`), bump `ea1cea0d197d4025c44c0d75c086583586d37a68`. `edge_intersects_region` antes cull raio fixo 11 contra bank 2.5x; agora usa maior raio real interpolado do segmento * 2.5. Lago cull expande maximum_horizontal_extent por LAKE_SHORE_OUTER_DISTANCE 1.18. Graph scan margem absoluta 16.5 falhava para rios largos por width multiplier de bioma; novo `sample_horizontal_with_radius_multiplier` considera cada raio real * 2.5, preservando normalized distance. Constante bank única em `hydrology/constants.rs`. Testes unitários adicionados em spatial e feature_graph; não executados. Pode ampliar carga do grafo por região; medir desempenho. Não fecha rios terminando no nada.

**.36 — alinhamento leito/fluido/carving no código, gameplay não confirmado:** math `057768e07d2a584436bf281b3a9bbb8442dd6a35`, water `cf9304bf0257c22e58045cf1e8fd9a4abd16bbf0`, density `9a0238713d0eb96695900cc251c132c6a8eff841`, bump `13bc930ae794f021d93695ed0318b64336412366`. Água de rio existia só na força graph >0.25 (distância normalizada <0.75), carving antigo até <1.0 criava faixa seca deprimida 0.75..1.0; perfil de profundidade divergia. Novo owner `river_channel_profile`, limite compartilhado `RIVER_WATER_BOUNDARY_NORMALIZED_DISTANCE`, escavação limitada a água real e mesmo perfil de leito. Margem externa preservada. Testes unitários adicionados, não executados. Caso de descida abrupta pode persistir por `generation/fluids.rs::surface_water_is_supported`: filtro exige `surface_height + 0.5 >= bed_level`, de modo que um canal elevado sobre vale perde água, intencionalmente evita fluido suspenso; investigar geometria/bed por seed/coords sem simplesmente remover suporte.

**.35 — mountain belt estreitado, gameplay não confirmado:** `data/dimensions/overworld/biomes/mountains.json` commit `7e3d509a51825e65fdc8956e8ed90d35804353c3`, bump `ef172294b2c0ec824eba4c2a1abb33cceda5f462`; threshold `.90 -> .965`, width `.15 -> .08`, região macro positiva `abs(noise)<.25 -> <.115`. CI .35 success. Não prova diversidade biome numa caminhada 2000 blocos; amostrar primary_id por seed/coords e comparar aparência. Preservar quatro biomas regionais.

**.34/.33:** .34 unused import `VoxelChunk` corrigido, CI success; .33 captura e reconciliação do halo vizinho após mesh inicial por `needs_initial_catchup`, notificação de 26 vizinhos filtrados por borda Geometry/Fluid, sem confirmação gameplay, pode custar FPS. `ChunkRemeshQueue::enqueue_lighting_change` preserva coords não renderizados; `DeduplicatedQueue::pop_where` preserva entradas adiadas; remesh `LightingRemeshDependencies` já verifica revisões de 27 chunks, não duplicar sem prova. `.32` `setPhase` da Lua Overworld Dawn→Night para sumir em Dawn, CI success sem gameplay; lua seguindo câmera é separado (`celestial.rs`: camera_position+celestial_offset). `.31` invalidação diagonal na iluminação CI success, defeito visual relatado persistiu. **ÚNICO FECHAMENTO DESTE CICLO CONFIRMADO PELO USUÁRIO: céu por bioma corrigido em .24.** Históricos detalhados nos links acima.

## Nove defeitos relatados 16/09 — ABERTOS até confirmação

| # | Prioridade | Defeito / aceite |
| --- | --- | --- |
| 1 | P0 | Chunks pretos inteiros, sombras/costuras nas bordas pós-.31. .33 halo e .38 seed antecipado corrigidos no código, não confirmados visualmente; discriminar persistentes de transitórios e medir FPS. |
| 2 | P0 | Rios terminam no nada. Provar grafo, leito e água FÍSICA end-to-end por seed/coords cruzando regiões/chunks; .15/.16/.19/.20/.37 não bastam para validar. |
| 3 | P0 | Em 2000 blocos apenas Plains/Mountains; .35 estreitou mountain belt, não confirmado. Amostrar primary biome por seed/coords, comparar aparência. |
| 4 | P0 | Em descidas margem abaixa ANTES da água; .36 alinha carve/bed/footprint e .37 corrige cortes na fronteira, testar gameplay e filtro do fluid pass; margens gerais .12 preservadas. |
| 5 | P1 | Junção rio-lago abrupta; suavizar XY/largura/profundidade/margem/altura preservando endpoints/conectividade. |
| 6 | P1 | Túneis paredes retas ao abrir superfície; cave connector/density/carver; cruzamento rio-túnel parede é bug adicional. |
| 7 | P1 | Lua parece seguir câmera; referencial `celestial.rs` identificado mas visual ainda não solucionado. |
| 8 | P1 | Lua visível durante dia; .32 setPhase Night alterado, sem gameplay. |
| 9 | P1 | Highlight target não envolve todas camadas de textura; inspecionar bounds/alpha/overlays. |

Outras pendências: `fogColor` artístico, FPS/streaming sob deslocamento sem benchmark confiável ~60, R/held .18 CI green sem gameplay, parede cave-river. Player HUD planejado inferior esquerdo `Yogg'Sara`, vida `50 / 100` na barra, status acima; target HUD acima crosshair, ícone esquerda estilo slot inventory, texto direita sombra tooltip. Históricos resolvidos conforme usuário: céu .24, margens gerais .12, hotbar vazia, ghost alpha uniforme, dye, HUD histórico, fog frontier/nuvens; não regredir.

## Próxima execução

1. Conferir CI .38 push `35141651000` Clippy/Check e corrigir qualquer erro/warning sem suprimir, bump/handoff se alterar código. Não declarar testes unitários ou gameplay executados.
2. P0 #1: distinguir chunks pretos que persistem de escurecimento transitório sob deslocamento. Auditar inicial/relax e sample do halo sem ar, mesh async e luz por coluna; não compensar com brilho global. Medir carga introduzida pelo seed antecipado com perf real, sem regredir streaming.
3. P0 #2/#4: provar rio end-to-end em seed/coords, grafo regional, água horizontal, leito, density, `surface_water_is_supported`, fluido físico e confluências. `river/path/terrain.rs` restringe altura com mediana de centro/bancos e mínimo do endpoint de jusante; pode deixar água acima de piso de vale, mas não presumir sem teste. Não remover filtro de suporte e criar água suspensa.
4. P0 #3 amostrar primary_id e aparência em trajetórias; depois túneis, lua, highlight. A cada bloco subir VERSION e atualizar HANDOFF mesma sessão. Não fechar nenhum dos nove sem confirmação gameplay.
