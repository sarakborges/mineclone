# HANDOFF — Asteria / Mineclone

**Fonte canônica:** `sarakborges/mineclone`, branch `develop`; Rust + Bevy 0.19.1. **Versão raiz `VERSION`: `0.20.3`**: bloco de save aberto, não subir sem QA real. `Cargo.toml` `0.10.16` tem versionamento independente. **Último código verde:** `f55f258429769bb072de571ee966f4159b9a0583`, CI https://github.com/sarakborges/mineclone/actions/runs/35252851074: auditoria dos três idiomas, Clippy `--all-targets --all-features -- -D warnings` e `cargo check --locked` concluídos com sucesso. CI não comprova execução/roundtrip no Windows.

## História integral obrigatória (não descartar requisitos)

- [Etapas 0–12](docs/handoffs/auditoria-0-12-2026-09-17.md), blob `34161a1ec793041a738ec704e41dc345cc87fff6`.
- [Etapas 13–16](docs/handoffs/auditoria-13-16-2026-09-17.md), blob `cbbb6710e8be6b132467c943f03ed8df326509e3`.
- [Etapas 17–20](docs/handoffs/auditoria-17-20-2026-09-17.md), blob `bc4a5e7f5ff723cc6f52ef0c50edce73b37b5da2`.
- [Etapas 21–24](docs/handoffs/auditoria-21-24-2026-09-17.md), blob `8b35a7c3680f7df1d3aaf4e142fcf39ed605036d`.
- [Etapas 25–27 e requisitos de save](docs/handoffs/auditoria-25-27-2026-09-17.md), blob `681c00fbb1674687affd4cf87d06c9317213c707`.
- [Etapas 28–31 na íntegra](docs/handoffs/auditoria-28-31-2026-09-17.md), blob `9b46ae054ed205590d67a353fb5117c471e419a8`.
- [Etapas 32–36 na íntegra](docs/handoffs/auditoria-32-36-2026-09-17.md), blob `33ea041165e699a5a95480f60f9b84a26d33d005`.
- [Etapas 37–44 e handoff anterior integral](docs/handoffs/auditoria-37-44-2026-09-17.md), blob `7a097aac02ebbcad2b5cadf0a2a79dc3cf191b67`, arquivado sem cortes em `eb4d0346aed06db3110b704a57162726a45d3940`. Inclui SHAs, resultados CI, detalhes de scanner, serialização e trava. **Correção posterior:** o próximo item sobre prioridade de Back nesse arquivo foi concluído no commit `f06c68c` abaixo. Ler também `ARCHITECTURE.md`.

**Regras permanentes:** implementar commits coerentes na `develop`, atualizar este handoff a cada checkpoint (SHA, CI, limitações e próximos passos) e arquivar versão integral antes de condensar. Incrementar semanticamente `VERSION` após cada bloco funcional concluído, não por documentação isolada, e não fechar saves sem QA. Corrigir todos os warnings sem supressão; CI exige auditoria de PT-BR, espanhol e inglês, Clippy rigoroso e `cargo check --locked`. Não executar/adicionar `cargo test` sem autorização expressa. Não alterar PNG/GLB autorais. Pause NÃO congela o mundo, fluidos ou horário. `Cargo.lock` é gitignored, CI cacheia lock e compilados: preservar otimização. Não inventar execuções ou números de desempenho. Informar progresso concreto, sem prometer trabalho em segundo plano.

## Estado consolidado até etapa 44

UI de nome único e mundos isolados, manifesto/snapshot imutáveis com commit marcador, chunks alterados residentes/arquivados, IDs portáveis, inventário, posição, regras, dimensão e relógio; autosave após 60 segundos apenas quando assinatura de mundo/posição/inventário/regras mudou; Leave/Exit salvam antes de sair e permanecem no jogo se falhar. Fallback valida geração inteira inclusive inventário/relógio/chunks; retenção considera quatro gerações realmente recuperáveis. Prune fora do frame, catálogo verificado fora do frame com data do snapshot de fato recuperável, carga selecionada em worker. Registro de duração de capture/publication em `save_session.rs`; JSON buffered com limite 512 MiB, leitura buffered. Travas por mundo etapa 43; etapa 44 abriu todos os candidatos sob trava, decodificando fora dela. Histórico, CI e ressalvas exatas no arquivo 37–44.

## Etapa 45 — cancelamento prioritário e leitores de snapshots com limite [CÓDIGO + CI VERDE; QA WINDOWS PENDENTE]

- `f06c68c39563b07095f383b060639463b0aac659`, CI https://github.com/sarakborges/mineclone/actions/runs/35251176602 completed/success (idiomas, Clippy e cargo check): `src/screens/world_selection.rs` ordena `handle_world_selection` antes de `poll_world_load` e pré-varre ações Back antes de Select/Load; Back ganha mesmo se conclusão do worker e outros botões chegarem no mesmo frame. Marca `pending.abandoned` e não aplica resultado tardio. **Não afirmar que a interação foi testada no Windows.**
- `f55f258429769bb072de571ee966f4159b9a0583`, CI https://github.com/sarakborges/mineclone/actions/runs/35252851074 completed/success (idiomas, Clippy -D warnings, check): `src/world/save_catalog.rs` substitui abertura simultânea de todos os arquivos do fallback por `SnapshotCandidates` com `ReadLease` RAII/contador atômico por mundo. Enumeração e incremento do contador acontecem sob trava do mundo; decodificação ocorre sem mutex, abrindo **um snapshot de cada vez**. Podador verifica leitores sob a mesma trava antes de excluir e adia exclusão quando houver leitor ativo; assim não remove caminhos de fallback, inclusive no Windows. `WorldGate` agora também contém `prune_running` por mundo, evitando que cleanup demorado de A impeça iniciar cleanup de B. Arquivos imutáveis publicados continuam preservados, sem mudar formato do save. **Limitação intencional:** prune que encontrou leitor não fica reprogramado automaticamente; próxima gravação tenta de novo, logo backups extras podem permanecer indefinidamente em mundo sem novas edições. Worker de leitura/poda ainda decodifica mundos completos e usa CPU/RAM. Mapa de gates retém um objeto pequeno por mundo até fim do processo. Não houve teste de corrida, carga ou QA em Windows; `cargo test` não foi executado.

## Próximas etapas, em ordem

1. Resolver cleanup adiado por leitor **sem depender de outro save** e sem spin no frame, mantendo prioridade de integridade; limitar concorrência/CPU/RAM do scanner e da carga. Considerar worker esperando término do lease fora do mutex ou fila por mundo, com cancelamento/timeout apropriado. Verificar que não haja deadlock de lock + contador.
2. Instrumentar e aferir no Windows os logs `capture` e `publication` com mundo pequeno, grande e chunks arquivados; medir tempo de leitura, CPU e RAM antes de novas otimizações. Save atual ainda captura e faz escrita/`fsync` na thread principal, por contrato Leave/Exit somente sai após publicação confirmada.
3. QA Windows de navegação: Load de mapa grande, Back durante e no mesmo frame da conclusão, retorno à seleção, erro de snapshot corrompido, scan/load simultâneos, transição apenas após recursos íntegros. Testar poda durante leitores e gravações em dois mundos, inclusive após vários saves; confirmar retenção/descritores.
4. QA de roundtrip/restart: nome duplicado, mundo isolado, blocos/fluidos/brush em chunk arquivado, inventário, posição, regras, relógio, autosave, Leave, Exit, reinicialização; corromper geração mais recente e conferir fallback+timestamp; interromper entre snapshot/manifesto; simular falta de permissão, arquivos extras e crash na poda. Sem alegar verificação não executada.
5. Preservar CI/cache (`--locked`, `-D warnings`), nenhum `cargo test` sem autorização. Só depois de QA e fechamento funcional subir `VERSION` conforme major/minor/patch. `Cargo.toml` permanece independente até decisão explícita.
