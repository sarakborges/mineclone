# HANDOFF — Asteria / Mineclone

Repositório `sarakborges/mineclone`, branch **`develop`**, Rust + Bevy 0.19.1. **Fonte canônica persistente: atualizar na mesma sessão de QUALQUER alteração material.** Históricos íntegros: [pré-.31](https://github.com/sarakborges/mineclone/blob/72b60b5a4e70a9960b3108debdf77d7a8510c4b1/HANDOFF.md), [feedback .31](https://github.com/sarakborges/mineclone/blob/6b8051628aa49c3b01ffca782144b8fc84705365/HANDOFF.md), [estado .32](https://github.com/sarakborges/mineclone/blob/5900057c6e7ec8d53684bf877a47c499ae6d3b5b/HANDOFF.md), [implementação .33](https://github.com/sarakborges/mineclone/blob/a90e42a61d18a8aa4e326b17450f80e5b334de53/HANDOFF.md), [detalhes .34](https://github.com/sarakborges/mineclone/blob/814a59b86fe32c73655a7d27ee4142b3949bf0ae/HANDOFF.md), [estado .35](https://github.com/sarakborges/mineclone/blob/355c0f5eeae8d08738c49f7a895c2d9534714793/HANDOFF.md), [estado .36](https://github.com/sarakborges/mineclone/blob/154e4bfd0d050cb78a7e74c9c210b3aa9676c167/HANDOFF.md), [estado .37](https://github.com/sarakborges/mineclone/blob/1b90522fe0bde385bfa48c09fc93a00de54b0488/HANDOFF.md) e [estado completo .38](https://github.com/sarakborges/mineclone/blob/9ff4279337b6ba2af8d03274ee0d9be8918b75a3/HANDOFF.md). Compactação nunca fecha bug nem substitui histórico.

## Regras obrigatórias

`continua`/`go` significa executar, não só planejar. Conferir HEAD, VERSION, código real e CI antes de escrever; alterações DIRETO em `develop`, sem branches não solicitadas. Cada bloco de código/dados jogáveis sobe `VERSION` conforme SemVer (PATCH compatível, MINOR funcionalidade, MAJOR quebra); documentos isolados não sobem. `Cargo.toml` 0.10.16 é deliberado, não mudar por causa de VERSION. Atualizar HANDOFF **na mesma sessão** com versão, SHA, estado CI, bugs ainda abertos e próxima etapa. Erros e warnings reais primeiro, não silenciar. CI `cargo clippy --all-targets --all-features -- -D warnings` + `cargo check`, **não executa unit tests**; fmt não é gate. Nunca afirmar gameplay confirmado sem feedback/execução. Não repetir desculpas de cargo local ou espera por cargo run. Sem imagens não solicitadas; binários originais exatos.

## Arquitetura e invariantes

`ARCHITECTURE.md` autoritativo: owner único por fato, SystemParams por domínio, UI em `src/ui`, targeting `TargetedBlock` único; change-driven hot paths, `DeduplicatedQueue<T>`/`VoxelUpdateQueue`, `FrameWorkBudget`, evitar scans globais e dirty writes redundantes. Pipeline async visible/preload -> geração -> integração **e seed direto uma vez por residência (.38)** -> snapshot halo 3×3×3 -> mesh -> render/visibilidade -> archive/unload. Visible supera preload, preempção devolve não crítico, stale outputs sem starvation. Terreno, fluid e luz possuem meshes/filas separadas. `ChunkRemeshTasks::lighting_revisions` é distinto de `VoxelWorld::chunk_mesh_revisions`; reconciliação inicial pós-publicação quando halo chega. HDR world order0 Skip; viewmodel order1 tonemap final; UI SDR order2. Céu por bioma/relógio vem de `EnvironmentVisualState.sky_color`, ClearColor exato; fogColor artístico pendente; nuvens world-space; sol/lua órbitas e rise/set independentes data-driven. Hidrologia com endpoints X/Z/Y compartilhados e margens gerais .12 preservadas, água natural autoritativa sem enfileirar massa no solver. Held block observer da hotbar, R apenas colocação.

## Estado 16/09/2026

**VERSION = 0.15.39.** Código `.39` em `src/voxel/mesh_lighting.rs`: commits `9974d365c8a66a8aab818ad50d83ea669566817d` (implementação inicial) e `9b7ee42d8256a1cd57b35168d942bc769179c0d3` (correção IMEDIATA de dois erros de transcrição, código final); VERSION bump `822fefc5ea735f85174312e6e5787d43d8b30fc4`. HANDOFF commit posterior: consultar HEAD. **CI .38 SUCCESS** push run `35141651000`, job `104947360899`, Clippy e Check SUCCESS. **CI .39 push run `35142554264` em andamento no registro, PR `35142559395` em andamento: consultar ambos; não assumir aprovação.** CI .37 push `35140172992` Clippy/Check SUCCESS. Nenhuma CI executa unit tests ou gameplay. `GitHub.compare_commits` `.38` handoff `9ff4279` -> `.39` código final `9b7ee42`: somente `src/voxel/mesh_lighting.rs` (37 adições, 3 remoções). Atenção: commit intermediário `.39` `9974d36` tinha placeholder inválido `left_dummy_placeholder` e vetor Left incorreto, ambos já corrigidos no commit `9b7ee42` ANTES do bump; só referenciar conteúdo final.

**.39 — luz de teto provisória para fronteira não carregada, hipótese de defeito visual delimitada, gameplay NÃO confirmado.** Inspeção: `mesh/geometry.rs::is_face_exposed` emite faces contra vizinhos não carregados, `mesh_lighting.rs::average_shader_light_levels` ignorava quatro amostras ausentes e retornava luz do céu zero; uma face superior na fronteira entre chunks podia ficar toda preta antes do chunk acima existir. Novo `provisional_top_sky_sample(face, sample)` só substitui **base ausente E face Top** por amostra sintética de céu 15/bloco 0, **apenas nos dados temporários da malha**, preservando ar real carregado, obstrução/caverna carregada, faces laterais/Bottom, AO, propagação e voxel light autoritativo. Quando upper chunk chega, reconciliação halo .33 e notificação no streaming atualizam a malha. Teste unitário `only_unloaded_top_faces_receive_provisional_sky` verifica top ausente, laterais/bottom ausentes e amostra escura válida preservada; teste ainda NÃO executado. Não afirmar correção de chunks inteiros pretos: esta solução se restringe à face superior sem vizinho. Medir visualmente top frontier sob caminhada, persistência de chunks pretos e FPS; não clarear cavernas globalmente.

**.38 — ordem seed de streaming corrigida no código, gameplay NÃO confirmado:** `src/world/streaming.rs` commit `1d76fae2f7f6ad99fd2d43f318432b9ec519538e`, VERSION bump `74b75bca2fd82ee7af39e8386b37a616945799d0`, handoff `.38` link acima. `VoxelChunk::empty` inicia DARK e integração anterior só semeava em `dispatch_initial_mesh_tasks` APÓS verificar capacidade: snapshot de outro chunk via halo podia capturar chunk recém-integrado ainda escuro. `seed_loaded_chunk_lighting` unifica execução exatamente uma vez/residência, incluindo fluido frontier, seed direta, reconciliação de meshes vizinhas in-flight e relaxação; agora chamada na integração após `insert_chunk` ou restore e no caminho de restore da dispatch. `dispatch_initial_mesh_tasks` fallback antes de checar capacidade, normalmente no-op. **Risco**: seed direto foi para orçamento integração geração 1ms/8 resultados; medir frame time, throughput, mesh backlog, FPS. Correção do estado transitório não comprova causa dos chunks persistentes.

**.37 — margens regionais no código, gameplay NÃO confirmado:** commits `8b2ea3a64f27f32d75bdb1643add9e47b424468e` (`constants.rs`), `56d237034969a44d3c1828ba1798452eed75c1a1` (`spatial.rs`), `e069ac54fd268035b95d2b97c2b8caf571aed408` (`river/path/confluence.rs`), `a4c5333a16ab384b64cc911fb2d5f463bcae7268` (`feature_graph.rs`), `3e10cde00725446769cd30edf0607ae34f218178` (`density.rs`), VERSION bump `ea1cea0d197d4025c44c0d75c086583586d37a68`. `edge_intersects_region` raio fixo 11 perdia river bank até 2.5×; agora usa maior raio interpolado real do segmento × 2.5. Lago cull expande `maximum_horizontal_extent` por `LAKE_SHORE_OUTER_DISTANCE=1.18`. Graph sampling por raio individual × 2.5 respeita width multiplier bioma, diferente da antiga margem absoluta 16.5; density usa mesma constante bank de owner único. Testes adicionados spatial/feature_graph não executados; avaliar custo de grafo/região durante streaming. **Não fecha rios terminando no nada.**

**.36 — leito, fluido e escavação alinhados em código, não confirmado visualmente:** commits math `057768e07d2a584436bf281b3a9bbb8442dd6a35`, water `cf9304bf0257c22e58045cf1e8fd9a4abd16bbf0`, density `9a0238713d0eb96695900cc251c132c6a8eff841`, VERSION bump `13bc930ae794f021d93695ed0318b64336412366`. Água rio graph strength >.25 = normalized distance <.75, mas carve era <1.0: faixa seca deprimida; profundidade também divergia. `river_channel_profile` e `RIVER_WATER_BOUNDARY_NORMALIZED_DISTANCE` unificam leito e carve, margem externa preservada. `generation/fluids.rs::surface_water_is_supported` pode rejeitar em descida quando `surface_height + 0.5 < bed_level`; não retirar filtro para criar água suspensa sem geometria correta.

**.35** `mountains.json` commit `7e3d509a51825e65fdc8956e8ed90d35804353c3` e VERSION bump `ef172294b2c0ec824eba4c2a1abb33cceda5f462`: mountain belt threshold .90→.965, width .15→.08, espaço de influência macro positivo `abs(noise)<.25`→`<.115`; CI success, sem confirmação de diversidade visual. **.34** corrigiu Clippy unused import VoxelChunk, CI success; **.33** catchup halo 26 vizinhos pós-publicação e notificações por borda, sem confirmação de FPS/gameplay. Remesh lighting já guarda não renderizados e valida 27 revisões; NÃO duplicar tracking. **.32** Lua Overworld setPhase Dawn→Night para desaparecer no começo Dawn, CI success sem gameplay; Lua seguindo câmera ainda separado (`celestial.rs`). **.31** invalidação diagonal, CI success, problemas visuais persistiram pelo feedback. **ÚNICO FECHAMENTO DO CICLO CONFIRMADO PELO USUÁRIO: céu por bioma corrigido em .24.** Históricos nos links.

## Nove defeitos relatados 16/09 — TODOS ABERTOS até confirmação

| # | Prio | Sintoma / critério |
|---|---|---|
| 1 | P0 | Chunks pretos inteiros e costuras nas bordas; .33 halo, .38 seed, .39 Top frontier são correções parciais em código. Distinguir persistente de transitório, medir FPS. |
| 2 | P0 | Rios terminam no nada: provar grafo, leito, água **física** fim a fim em seed/coords, atravessando chunks/regiões; .37 não basta. |
| 3 | P0 | Em 2000 blocos apenas Plains/Mountains: .35 narrow belt não confirmado, amostrar `primary_id`/seed/coords e aparência. |
| 4 | P0 | Em descidas margem abaixa ANTES da água: .36/.37 candidatos, validar suporte do fluid pass, sem água suspensa; margens gerais .12 preservadas. |
| 5 | P1 | Junção rio–lago abrupta: suavizar XY/largura/profundidade/margem/altura mantendo endpoints. |
| 6 | P1 | Túneis paredes retas ao abrir superfície; parede no cruzamento rio–túnel adicional. |
| 7 | P1 | Lua parece seguir câmera, `celestial.rs` referencial. |
| 8 | P1 | Lua visível de dia: .32 data setPhase mudou, sem confirmação visual. |
| 9 | P1 | Target highlight não envolve todas as camadas da textura; inspecionar bounds/alpha/overlays. |

Outras: `fogColor` artístico, FPS sob deslocamento sem benchmark confiável ~60, R/held .18 sem gameplay, parede cave-river. Player HUD planejado inferior esquerdo `Yogg'Sara`, vida `50 / 100` dentro da barra, status acima; target HUD acima crosshair, ícone à esquerda como slot inventory e texto à direita com sombra tooltip. Itens históricos confirmados pelo usuário: céu .24, margens gerais .12, hotbar vazia, ghost alpha uniforme, dye e HUD/fog anteriores; não regredir.

## Próxima execução

1. Consultar CI .39 push `35142554264` (e PR `35142559395`), sanar erros/warnings, atualizar handoff com estado verificado. Os testes unitários adicionados não rodam na CI.
2. P0 #1: distinguir face superior escura de chunk realmente preto inteiro/persistente. Examinar snapshot `mesh_lighting`, iluminação de ar real, seed vertical, revision/queue/remesh; confirmar que `.39` não clareia caverna medida; medir FPS regressão `.38` e .39 visual.
3. P0 #2/#4: escolher seed/coords, comparar grafo e água entre regiões, water_at, perfil do leito, density, suporte e geração `fluids.rs`; detectar pontos em que canal termina fisicamente e corrigir sem água flutuante. Verificar junction com lagos.
4. P0 #3: amostrar `BiomeField::sample_surface().primary_id` em trajetórias determinísticas e comparar resultados visuais. Depois P1 túneis/lua/highlight. Cada bloco de código ou dados sobe VERSION e HANDOFF na mesma sessão. Nenhum dos nove fechado sem gameplay.
