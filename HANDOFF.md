# HANDOFF — Asteria / Mineclone

**Fonte canônica:** `sarakborges/mineclone`, branch `develop`; Rust + Bevy 0.19.1. **Versão raiz `VERSION`: `0.20.3`**: bloco de save aberto, não subir sem QA real. `Cargo.toml` `0.10.16` tem versionamento independente. **Último código verde:** `709c14c881734d453a1cef51069f3a559b8a1a3c`, CI https://github.com/sarakborges/mineclone/actions/runs/35255733070: auditoria dos três idiomas, Clippy `--all-targets --all-features -- -D warnings` e `cargo check --locked` concluídos com sucesso. CI não comprova execução/roundtrip no Windows.

## História integral obrigatória (não descartar requisitos)

- [Etapas 0–12](docs/handoffs/auditoria-0-12-2026-09-17.md), blob `34161a1ec793041a738ec704e41dc345cc87fff6`.
- [Etapas 13–16](docs/handoffs/auditoria-13-16-2026-09-17.md), blob `cbbb6710e8be6b132467c943f03ed8df326509e3`.
- [Etapas 17–20](docs/handoffs/auditoria-17-20-2026-09-17.md), blob `bc4a5e7f5ff723cc6f52ef0c50edce73b37b5da2`.
- [Etapas 21–24](docs/handoffs/auditoria-21-24-2026-09-17.md), blob `8b35a7c3680f7df1d3aaf4e142fcf39ed605036d`.
- [Etapas 25–27 e requisitos de save](docs/handoffs/auditoria-25-27-2026-09-17.md), blob `681c00fbb1674687affd4cf87d06c9317213c707`.
- [Etapas 28–31 na íntegra](docs/handoffs/auditoria-28-31-2026-09-17.md), blob `9b46ae054ed205590d67a353fb5117c471e419a8`.
- [Etapas 32–36 na íntegra](docs/handoffs/auditoria-32-36-2026-09-17.md), blob `33ea041165e699a5a95480f60f9b84a26d33d005`.
- [Etapas 37–44 e handoff anterior integral](docs/handoffs/auditoria-37-44-2026-09-17.md), blob `7a097aac02ebbcad2b5cadf0a2a79dc3cf191b67`, arquivado sem cortes em `eb4d0346aed06db3110b704a57162726a45d3940`. Inclui SHAs, resultados CI, detalhes de scanner, serialização e trava. Correção posterior: prioridade de Back foi resolvida em `f06c68c` abaixo. Ler também `ARCHITECTURE.md`.

**Regras permanentes:** implementar commits coerentes na `develop`, atualizar este handoff a cada checkpoint (SHA, CI, limitações e próximos passos) e arquivar versão integral antes de condensar. Incrementar semanticamente `VERSION` após cada bloco funcional concluído, não por documentação isolada, e não fechar saves sem QA. Corrigir todos os warnings sem supressão; CI exige auditoria de PT-BR, espanhol e inglês, Clippy rigoroso e `cargo check --locked`. Não executar/adicionar `cargo test` sem autorização expressa. Não alterar PNG/GLB autorais. Pause NÃO congela o mundo, fluidos ou horário. `Cargo.lock` é gitignored, CI cacheia lock e compilados: preservar otimização. Não inventar execuções ou números de desempenho. Informar progresso concreto, sem prometer trabalho em segundo plano.

## Estado consolidado até etapa 47

UI de nome único e mundos isolados, manifesto/snapshot imutáveis com commit marcador, chunks alterados residentes/arquivados, IDs portáveis, inventário, posição, regras, dimensão e relógio; autosave após 60 segundos apenas quando assinatura de mundo/posição/inventário/regras mudou; Leave/Exit salvam antes de sair e permanecem no jogo se falhar. Fallback valida geração inteira inclusive inventário/relógio/chunks; retenção considera quatro gerações realmente recuperáveis. Prune fora do frame, catálogo verificado fora do frame com data do snapshot recuperável, carga selecionada em worker. Registro de duração de capture/publication em `save_session.rs`; JSON buffered com limite 512 MiB e leitura buffered. Travas por mundo desde etapa 43; etapa 45 substituiu abertura simultânea de todos os candidatos (etapa 44) por lease e um snapshot por vez; etapa 46 retoma poda após leitura sem novo save. Etapa 47 descarta resultado abandonado fora do frame e sem reter `VoxelWorld` grande fora do menu. Histórico, CI e ressalvas originais no arquivo 37–44. **Protocolo de QA ainda NÃO EXECUTADO:** [docs/save-roundtrip-qa.md](docs/save-roundtrip-qa.md).

## Etapa 45 — cancelamento prioritário e leitores de snapshots com limite [CÓDIGO + CI VERDE; QA WINDOWS PENDENTE]

- `f06c68c39563b07095f383b060639463b0aac659`, CI https://github.com/sarakborges/mineclone/actions/runs/35251176602 completed/success (idiomas, Clippy e cargo check): `src/screens/world_selection.rs` ordena `handle_world_selection` antes de `poll_world_load` e pré-varre ações Back antes de Select/Load; Back ganha mesmo se conclusão do worker e outros botões chegarem no mesmo frame. A marcação de abandono foi refinada na etapa 47 para um estado de conclusão/cancelamento sincronizado com o worker. **Interação não testada no Windows.**
- `f55f258429769bb072de571ee966f4159b9a0583`, CI https://github.com/sarakborges/mineclone/actions/runs/35252851074 completed/success (idiomas, Clippy -D warnings, check): `src/world/save_catalog.rs` substitui abertura simultânea dos arquivos do fallback por `SnapshotCandidates` com `ReadLease` RAII/contador atômico por mundo. Enumeração e incremento sob trava; decodificação sem mutex, abrindo um snapshot por vez. Poda só pode excluir sem leitores. `WorldGate` contém `prune_running` por mundo, de modo que cleanup de A não impede iniciar cleanup de B. Formato do save não muda. A limitação original de adiar limpeza até próximo save foi resolvida na etapa 46. Workers ainda decodificam mundos completos e consomem CPU/RAM; mapa de gates retém um objeto pequeno por mundo até fim do processo. Nenhum QA de corrida/carga/Windows ou `cargo test` foi executado.

## Etapa 46 — limpeza retoma quando último leitor termina [CÓDIGO + CI VERDE; QA WINDOWS PENDENTE]

- `e01044b54c1843b811c164c984e72b4e7de2f07c`, https://github.com/sarakborges/mineclone/actions/runs/35254967476 completed/success: `WorldGate` ganhou `reader_signal: Mutex<()>` e `readers_finished: Condvar`; `ReadLease::drop` serializa decremento do contador com o mutex de condição e sinaliza ao liberar o último leitor. Após validar quatro gerações jogáveis fora da trava, o worker adquire `write`, verifica leitores e, se houver, libera `write` para aguardar pela condição sem busy-spin. Ao acordar readquire `write` e confere novamente o contador para impedir corrida com novo leitor. Só então apaga manifests antigos antes dos snapshots. Não depende de outro save nem ocupa a trava de publicação durante a espera. Diff confirmou mudança restrita à sincronização. Idiomas, Clippy rigoroso e cargo check passaram, sem `cargo test`.
- `18a4cc8d7a50939d9513f54cdc539c8c2d985b26` acrescentou [protocolo manual de QA Windows e roundtrip](docs/save-roundtrip-qa.md): cenários reproduzíveis de nomes duplicados, chunks arquivados, fluidos/brush, relógio, autosave, falha de escrita, corrupção/fallback/timestamp, interrupção de publicação, corrida de navegação, leitores/poda e medições. Documento não é prova de execução.
- **Limites:** worker de poda pode aguardar indefinidamente se leitor travar; não há timeout/cancelamento. Múltiplos mundos podem iniciar simultaneamente workers pesados de validação; falta orçamento/concorrência global. Snapshot capture, JSON e fsync ainda síncronos; nenhuma latência medida em Windows. Teste real de interleaving Condvar/ReadLease, reinício e recuperação pendente. `VERSION` `0.20.3`.

## Etapa 47 — descarte de load cancelado fora do frame [CÓDIGO + CI VERDE; QA WINDOWS PENDENTE]

- `bb667e634f1f819748350cbccd1f6b8fd4876800`: `WorldLoadResult` passou a conter `WorldLoadSlot { abandoned, complete, result }` sob um único `Mutex`, eliminando corrida entre cancelamento e publicação. Back e OnExit da seleção chamam `PendingWorldLoad::abandon`: marcam abandono e transferem eventual resultado já pronto para thread `asteria-discard-world` em vez de destruir `VoxelWorld` grande no frame. Se a conclusão chegar após abandono, o próprio worker destrói o resultado sem armazená-lo no recurso de tela. `poll_world_load` espera `complete`, descarta abandonados, impede aplicação durante transição e só aplica mundo válido na thread Bevy. Mantém rastreio de worker até conclusão para não iniciar outro antes dele terminar. `spawn` de thread de descarte pode falhar, situação que registra warning e cuja closure é descartada localmente; validar UX e pressão de memória no Windows.
- Primeira CI https://github.com/sarakborges/mineclone/actions/runs/35255555252 falhou em dois warnings: `unused_mut` em `abandon_world_load` e `collapsible_if` em descarte. `709c14c881734d453a1cef51069f3a559b8a1a3c` corrigiu sem supressão. **CI final:** https://github.com/sarakborges/mineclone/actions/runs/35255733070 completed/success — auditoria dos três idiomas, Clippy `-D warnings`, `cargo check --locked`; nenhum `cargo test`.
- Atenção ao fluxo visual: o caption `SelectionError` inicial agora começa vazio, e o sistema `sync_world_selection_feedback` preenche o erro no Update quando o recurso de estado muda. Conferir mensagem de falha no spawn durante QA de navegação; restaurar inicialização direta caso apareça atraso/flicker perceptível. Não alegar consumo de memória ou cancelamento medido sem execução.

## Próximas etapas, em ordem

1. Verificar por inspeção/QA se workers pesados de scan, load e prune devem compartilhar orçamento de concorrência, CPU e RAM sem sacrificar fallback nem bloquear frame. O descarte de load foi corrigido, mas o trabalho de IO/decodificação não é cancelado antecipadamente; somente seu resultado é descartado. Avaliar cancelamento cooperativo e prazo para a poda aguardar leases, sem espera ativa ou deadlock.
2. **Executar QA Windows** do [protocolo](docs/save-roundtrip-qa.md): coleta de logs `capture`/`publication`, tempos de load/scan, memória, CPU e teste de leitor + poda, incluindo dois mundos, Back no mesmo frame, erro de snapshot e transição. Nenhuma dessas medições foi feita pelo assistente.
3. QA de roundtrip e restart: nomes repetidos, blocos/fluidos/brush em chunk arquivado, inventário, posição, regras e relógio; autosave, Leave/Exit, geração recente danificada, timestamp de fallback, interrupção entre snapshot/manifesto, permissões e recuperação da poda. Registrar PASS/FAIL/NOT RUN sem supor resultado.
4. Com medições reais, otimizar gargalo identificado, preservando Leave/Exit: só sair após publicação confirmada, erro mantém mundo. `save_world` ainda captura e faz escrita/`fsync` no frame; não migrar sem contrato claro.
5. Preservar CI/cache (`--locked`, `-D warnings`), nenhum `cargo test` sem autorização. Só depois de QA e fechamento funcional subir semanticamente `VERSION`; `Cargo.toml` permanece independente até decisão expressa.
