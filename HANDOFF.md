# HANDOFF — Asteria / Mineclone

Repositório `sarakborges/mineclone`, branch `develop`, Rust + Bevy 0.19.1. **Esta é a fonte canônica persistente.** Históricos íntegros: [pré-.31](https://github.com/sarakborges/mineclone/blob/72b60b5a4e70a9960b3108debdf77d7a8510c4b1/HANDOFF.md), [feedback .31](https://github.com/sarakborges/mineclone/blob/6b8051628aa49c3b01ffca782144b8fc84705365/HANDOFF.md), [estado .32](https://github.com/sarakborges/mineclone/blob/5900057c6e7ec8d53684bf877a47c499ae6d3b5b/HANDOFF.md), [implementação .33](https://github.com/sarakborges/mineclone/blob/a90e42a61d18a8aa4e326b17450f80e5b334de53/HANDOFF.md), [detalhes .34](https://github.com/sarakborges/mineclone/blob/814a59b86fe32c73655a7d27ee4142b3949bf0ae/HANDOFF.md), [estado completo .35](https://github.com/sarakborges/mineclone/blob/355c0f5eeae8d08738c49f7a895c2d9534714793/HANDOFF.md) e [estado completo .36](https://github.com/sarakborges/mineclone/blob/154e4bfd0d050cb78a7e74c9c210b3aa9676c167/HANDOFF.md). Compactação nunca fecha bug.

## Regras obrigatórias

`go`/`continua` = executar, não só planejar. Consultar HEAD, `VERSION` e código real antes de escrever. Trabalhar em `develop` diretamente, sem branches não solicitadas. Todo bloco de código ou dados jogáveis sobe `VERSION`: PATCH correção/refactor compatível, MINOR funcionalidade compatível, MAJOR quebra de contrato. Documento isolado não sobe. `Cargo.toml` versão antiga 0.10.16 é deliberado. **Atualizar este HANDOFF na MESMA SESSÃO** de toda mudança material de código, versão, feedback, roadmap, processo e arquitetura, incluindo SHA, status CI e próximo passo. Erros/warnings reais primeiro, sem silenciar. CI canônico `cargo clippy --all-targets --all-features -- -D warnings` e `cargo check`, não executa unit tests, `fmt` não é gate. Não repetir cargo local/espera de `cargo run`; teste manual somente quando solicitado. Distinguir hipótese, código publicado, CI aprovado e gameplay confirmado. Sem geração de imagens não solicitadas; assets binários do usuário exatos.

## Arquitetura/canon

`ARCHITECTURE.md` autoritativo: owner único por fato, SystemParams por domínio, UI em `src/ui`, targeting `TargetedBlock` único, hot paths change-driven e invariantes reais compartilhados sem abstração vazia. Usar `DeduplicatedQueue<T>`/`VoxelUpdateQueue` e `FrameWorkBudget`, evitar scans globais, alocações, dirty writes supérfluos. Pipeline async seleção visible/preload -> geração -> integração -> seed direta UMA vez por residência -> captura halo 3×3×3 -> mesh -> render/visibilidade -> retenção -> archive/unload. Visible supera preload, preempção devolve não crítico, outputs stale protegidos sem starvation. Geometry/Terrain e Fluid usam meshes, filas e iluminação independentes. `ChunkRemeshTasks::lighting_revisions` ≠ `VoxelWorld::chunk_mesh_revisions`. Mesh inicial aparece rápido, depois reconcilia halo se necessário.

HDR world order0 Skip, viewmodel order1 tonemap final, UI SDR order2; céu por bioma/relógio em `EnvironmentVisualState.sky_color`, `SkyPlugin` aplica ClearColor EXATO após track biome. `fogColor` artístico separado ainda não aplicado; fog terminal precisa casar com sky plano sem mistura fixa. Nuvens world-space, vento independente, seaLevel por dimensão; sol/lua rise/set e órbitas independentes data-driven. Hidrologia endpoints incidentes X/Z/Y compartilhados, margens gerais .12 preservadas, determinismo entre regiões, água natural autoritativa e não enfileirar massa no solver. Held block observer de hotbar/tint, tecla R apenas colocação.

## Estado 16/09/2026

**VERSION = 0.15.37.** Commits publicados diretamente em `develop`: `8b2ea3a64f27f32d75bdb1643add9e47b424468e` (`src/world/hydrology/constants.rs`), `56d237034969a44d3c1828ba1798452eed75c1a1` (`src/world/hydrology/spatial.rs`), `e069ac54fd268035b95d2b97c2b8caf571aed408` (`src/world/hydrology/river/path/confluence.rs`), `a4c5333a16ab384b64cc911fb2d5f463bcae7268` (`src/world/feature_graph.rs`), `3e10cde00725446769cd30edf0607ae34f218178` (`src/world/hydrology/region/density.rs`), bump `ea1cea0d197d4025c44c0d75c086583586d37a68`. HANDOFF commit posterior: consultar HEAD. **CI .37 push run `35140172992` em andamento ao consultar; PR run `35140177714` enfileirado. Confirmar Clippy e Check; sem sucesso presumido. CI .36 push run `35139112147`, job `104938867724`: Clippy e Check SUCCESS.** Tests unitários NÃO executados no CI; gameplay NÃO confirmado.

**.37 — correção de inconsistência comprovada no código entre recorte de região e footprint de margens, visualmente não confirmada:** `spatial::edge_intersects_region` recortava segmentos de rio usando margem fixa `RIVER_MAXIMUM_RADIUS` (11), mas `density::river_shore_normalized_distance` estende a margem a distância normalizada 2.5; segmentos com margem dentro da região podiam sumir por inteiro de seu `FeatureGraph`. `river/path/confluence.rs::add_path_to_graph` agora calcula os raios interpolados REAIS antes do cull e usa o maior raio do segmento multiplicado pelo limite externo 2.5. O limite `RIVER_BANK_OUTER_NORMALIZED_DISTANCE` possui owner único em `hydrology/constants.rs`; `region/density.rs` e o culling o compartilham. `spatial::water_body_intersects_region` antes usava só `body.maximum_horizontal_extent()` (já inclui irregularidade máxima 1.42); agora amplia por `LAKE_SHORE_OUTER_DISTANCE` (1.18), igual ao cull por coluna de density. A busca no `FeatureGraph` tinha segunda discrepância: `sample_horizontal_with_margin` aplicava margem absoluta `RIVER_MAXIMUM_RADIUS*1.5`, insuficiente para rios mais largos por `BiomeHydrology.river_width_multiplier > 1`. Novo `sample_horizontal_with_radius_multiplier` reutiliza o loop/hot path existente e multiplica o raio próprio de CADA aresta por 2.5, preservando `normalized_distance` no raio original; `sample_horizontal`/`sample_horizontal_with_margin` antigos preservam semântica. `density.rs` usa nova amostra. Testes unitários adicionados em `spatial.rs` para margem de rio, largura real e margem de lago; `feature_graph.rs` para rio largo que margem fixa descartava. **Não afirmar que rios acabam no destino, nem fechar problemas visuais.** Monitorar custo de scan/grafo/remesh na caminhada; não expandir seleção indiscriminadamente. `GitHub.compare_commits` base `.36` handoff `154e4bf` → commit código `.37` `3e10cde`: 5 arquivos de código, alterações delimitadas; VERSION separada.

**.36 — discrepância real entre leito, água física e margem corrigida no código, NÃO confirmada visualmente:** commits `057768e07d2a584436bf281b3a9bbb8442dd6a35` (`src/world/hydrology/math.rs`), `cf9304bf0257c22e58045cf1e8fd9a4abd16bbf0` (`src/world/hydrology/region/water.rs`), `9a0238713d0eb96695900cc251c132c6a8eff841` (`src/world/hydrology/region/density.rs`), bump `13bc930ae794f021d93695ed0318b64336412366`. `HydrologyRegion::water_at` só selecionava rio se graph `strength > SHORE_STRENGTH` (0.25), equivalente ao raio normalizado `< 0.75`, enquanto `density_column_profile` escavava até `< 1.0`. A faixa `0.75..1.0` podia ser cavada sem receber fluido. O perfil de profundidade em `water.rs` ainda usava `smoothstep(strength)` e a escavação usava `smoothstep(1-distance)`, divergindo. Introduzido `math::RIVER_WATER_BOUNDARY_NORMALIZED_DISTANCE` e `river_channel_profile(normalized_distance)`, owner único para perfil do leito. Escavação agora exige distância `< 0.75`, profundidade de água e carve usam exatamente o perfil compartilhado; `water_near` usa distância física no leito sem permitir que a margem de consulta infle profundidade. A margem externa permanece regida por `river_shore_normalized_distance` até distância 2.5 e por `shore_density_delta`. Não alteradas configurações de lago/oceano ou solver. Testes de limite adicionados em `math.rs` e `region/density.rs`, NÃO executados no CI. Candidata para margem que afunda antes da água, mas pode não explicar todo caso em descida abrupta; bug #4 aberto até gameplay por seed/coords. Inspecionar suporte da coluna em `src/world/generation/fluids.rs::surface_water_is_supported` se houver trecho escavado sem fluido mesmo com `water_at` positivo. Preservar margens gerais confirmadas na .12.

**.35 — tentativa delimitada de restaurar espaço para biomas regionais, NÃO confirmada no gameplay:** `data/dimensions/overworld/biomes/mountains.json` commit `7e3d509a51825e65fdc8956e8ed90d35804353c3`, bump `ef172294b2c0ec824eba4c2a1abb33cceda5f462`. `mountain_belt_strength` usa `ridge = 1 - abs(fractal_noise)`, intensidade positiva para `ridge > threshold - width` e domina `sample_surface` quando influência macro vence regional. Overworld antes threshold .90 width .15 => `abs(noise) < .25`; depois threshold `.965` width `.08` => `abs(noise) < .115`. Narrowing reduz área macro, preserva scale/warp, peaks, elevação, cavernas, pesos e definições dos quatro biomas regionais. CI .35 push run `35138210276`, job `104935826429`: Clippy e Check SUCCESS. Não demonstra 4+ biomas numa caminhada de 2000 blocos: amostrar `primary_id` por seed/coords e comparar aparência. Se ainda Plains/Mountains, distinguir seleção real de aparência, olhar `BiomeField::sample_surface`, `select_surface_biome_index`, pesos/distribuição.

**.34 CI VERDE:** push run `35137428833`, job `104933180184`, Clippy e Check success. Corrigido unused import de `VoxelChunk` ao mover para `#[cfg(test)] mod tests`, commit `0bc584505742cf3696308f265560eb3c6bd80571`, bump `50fc8a1dc3011323f581cdd44bd6fc044cabdd25`. .33 CI run `35136774532` falhou somente no import (Check skipped); .32 CI `35136271200` job `104929320897` success; .31 `35134931146` job `104924817286` success. .24–.30 CI success segundo históricos; .22/.23 falharam antes das correções.

**.33 reconciliação inicial:** `src/voxel/mesh_snapshot.rs` commit `02eb6d233804287f72b6084b803d024c256523fb` implementa `needs_initial_catchup` quando um dos 26 vizinhos estava ausente na captura e presente na publicação. `src/world/streaming.rs` commit `6ff41717948bffa1f88a252910214c7e125fe359`: marca meshes vizinhas in-flight no seed para catch-up depois de first-visible sem re-seed/cancel; notifica 26 vizinhos renderizados com filtro Geometry/Fluid pelas bordas. Testes unitários adicionados, CI não os executa. Pode aumentar remesh no streaming; medir FPS. Gameplay ainda não confirmado.

**Correção de hipótese .34:** não presumir que toda notificação de luz de mesh inicial in-flight se perde. `ChunkRemeshQueue::enqueue_lighting_change` guarda coords mesmo não renderizados; `DeduplicatedQueue::pop_where` preserva não elegíveis (`pop_where_preserves_deferred_entries`). `ChunkRemeshTasks::LightingRemeshDependencies` já captura/valida 27 revisões; NÃO duplicar tracking sem reprodução. `needs_initial_catchup` enfileira remesh para qualquer halo novo mesmo se fronteira não contém conteúdo/fluido; filtrar se profiler provar custo. `average_shader_light_levels` retorna zero quando as quatro amostras não são ar carregado; pode afetar frontier, não comprovado como causa de chunks inteiros pretos; não clarear cavernas indiscriminadamente. Auditar seed/relaxação com seed/coords. Nenhum bug de iluminação visualmente confirmado corrigido.

**.32 lua diurna código:** `progress_between_phases` inclui duração inteira do `setPhase`; lua Overworld tinha `setPhase=Dawn` e permanecia todo amanhecer; alterado para `setPhase=Night`, some no início de Dawn. Commit `7d00f21a8f56b4b06befc85d846afd2b9601131f`, CI green, gameplay sem confirmação. Lua seguindo câmera é defeito distinto: `celestial.rs` usa `camera_position + celestial_offset`; investigar referencial visual sem mover esfera para perto do clipping. Sol/órbitas data-driven preservados.

**.31 iluminação diagonal parcial:** `src/world/lighting_updates.rs` invalida diagonais no relax com filtros Geometry/Fluid, commit `1965466e35683c0775d3a7b79e3bfaf37a49606f`, CI green; usuário confirmou que iluminação e demais defeitos persistiram após .31. **ÚNICO FECHAMENTO DESTE CICLO CONFIRMADO PELO USUÁRIO: céu por bioma corrigido em .24.** `fogColor` artístico ainda aberto.

## Nove defeitos relatados 16/09 — ABERTOS até confirmação

| # | Prioridade | Defeito / aceite |
| --- | --- | --- |
| 1 | P0 | Chunks pretos inteiros, sombras/costuras nas bordas pós-.31; .33 halo reconciliado no código, sem gameplay. Investigar seed/samples/task/queue e FPS. |
| 2 | P0 | Rios terminam no nada. Provar grafo, leito e água FÍSICA end-to-end por seed/coords cruzando regiões/chunks; .15/.16/.19/.20/.37 não bastam para validar. |
| 3 | P0 | Em 2000 blocos apenas Plains/Mountains; .35 estreitou mountain belt, não confirmado. Amostrar primary biome por seed/coords, comparar aparência. |
| 4 | P0 | Em descidas margem abaixa ANTES da água; .36 alinha carve/bed/footprint e .37 elimina cortes de margem em regiões, testar gameplay e suporte do fluid pass; margens gerais .12 preservadas. |
| 5 | P1 | Junção rio-lago abrupta; suavizar XY/largura/profundidade/margem/altura preservando endpoints/conectividade. |
| 6 | P1 | Túneis paredes retas ao abrir superfície; cave connector/density/carver; cruzamento rio-túnel parede é bug adicional. |
| 7 | P1 | Lua parece seguir câmera; referencial `celestial.rs` identificado mas visual ainda não solucionado. |
| 8 | P1 | Lua visível durante dia; .32 setPhase Night alterado, sem gameplay. |
| 9 | P1 | Highlight target não envolve todas camadas de textura; inspecionar bounds/alpha/overlays. |

Outras pendências: `fogColor` artístico, FPS/streaming sob deslocamento sem benchmark confiável ~60, R/held .18 CI green sem gameplay, parede cave-river. Player HUD planejado inferior esquerdo `Yogg'Sara`, vida `50 / 100` na barra, status acima; target HUD acima crosshair, ícone esquerda estilo slot inventory, texto direita sombra tooltip. Históricos resolvidos conforme usuário: céu .24, margens gerais .12, hotbar vazia, ghost alpha uniforme, dye, HUD histórico, fog frontier/nuvens; não regredir.

## Próxima execução

1. CI .37 push run `35140172992`: conferir Clippy/Check, sanar erros/warnings se houver com novo bump e handoff. Não afirmar unit tests ou gameplay executados.
2. Investigar P0 chunks pretos em seeds/coords e pipeline de luz; discriminar ausência de ar válido em `average_shader_light_levels` de chunk inteiro. Não adicionar brilho global ou revisões duplicadas sem evidência.
3. Examinar rio end-to-end e .36/.37 sob descidas: `region/water.rs`, `density.rs`, `generation/fluids.rs::surface_water_is_supported`, river `selection.rs`; comparar grafo, footprint, bed, surface, density e fluido por seed/coords. Separar grafo regional divergente de água física ausente; verificar confluências e junction com lagos. Medir custo da nova região com banks mais amplos se FPS cair.
4. Amostrar distribuição final de biomas em trajetórias determinísticas para validar .35; depois túneis, lua, highlight. Cada bloco de código/dados sobe versão e sincroniza handoff na mesma sessão. Não fechar defeitos sem comprovação gameplay.
