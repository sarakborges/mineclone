# HANDOFF — Asteria / Mineclone

Repositório `sarakborges/mineclone`, branch autoritativa de trabalho de mundo **`develop`**, Rust + Bevy **0.19.1**. Atualizar este HANDOFF na MESMA sessão de cada mudança material. Histórico anterior preservado nos snapshots: [0.15.50](https://github.com/sarakborges/mineclone/blob/b12e4ddd4f29c31f3cf21ce2f8ca70d237af8a74/HANDOFF.md), [0.15.49](https://github.com/sarakborges/mineclone/blob/98d5e1b181e3a8b317d24d90195a3495f61d53d4/HANDOFF.md), [0.15.48](https://github.com/sarakborges/mineclone/blob/8cdebad5fabbe55b93a2bfa45f4fd9e93dbf4739/HANDOFF.md), [0.15.47](https://github.com/sarakborges/mineclone/blob/457de45c0dc04e40a52ef6f5d567444e2b4f30da/HANDOFF.md), [0.15.46](https://github.com/sarakborges/mineclone/blob/c778fee85c9b4cbc10a38be9e413bfda9174eaab/HANDOFF.md), [0.15.45 e links .31–.44](https://github.com/sarakborges/mineclone/blob/46df141fe88ba7f96977482906362a189abab7e0/HANDOFF.md). Compactar o relato não fecha defeitos. Consolidação documental de branches realizada sobre o HEAD anterior de `develop` `43f33c305a3e6428de7e5291b2110c1b9662f2fc` em 16/09/2026; o commit deste documento é posterior.

## Regras obrigatórias

- `continua`/`go`/`continua com roadmap`: consultar HEAD, `VERSION`, arquivos reais, CI e este documento; continuar com mudanças reais justificadas. Para trabalho normal no projeto/mundo, preservar o fluxo estabelecido de alterações em `develop` quando solicitado. **Exceção contextual, NÃO regra global:** a instrução de criar/usar branches isolados foi exclusiva às features de criaturas, chat e edição de texto e respectivas PRs; não importar a proibição geral de escrever em `develop` contida no handoff do chat. Não fazer merges de código de PRs DRAFT ao consolidar documentação.
- Alteração de código, dados jogáveis ou testes relevantes exige bump do `VERSION` raiz segundo SemVer: patch fixes/refactor/testes, minor feature compatível, major quebra. `Cargo.toml` versão independente `0.10.16` na `develop`, **não** sincronizar com `VERSION`. Alterações isoladas de documentação não exigem bump; versões de branches não são versões publicadas em `develop`.
- Atualizar este HANDOFF na mesma sessão: branch, versão, SHA do último código, commits, validação efetiva, limitações, nove bugs e próximo passo. Conferir HEAD antes de escrever; não sobrescrever mudanças alheias.
- CI `.github/workflows/ci.yml` em .51 passou a executar **Clippy `cargo clippy --all-targets --all-features -- -D warnings`**, **`cargo check`**, e **`cargo test --bin asteria -- --test-threads=2`**. Antes de .51 não havia etapa de testes. A CI de branches antigos pode usar workflow diferente: conferir por SHA. Não alegar testes aprovados enquanto etapa não concluir. CI não cobre gameplay visual, FPS, Windows ou cenários grandes por si só; investigar e corrigir erros/warnings concretos.
- `ARCHITECTURE.md` é o cânone. Ownership único, `SystemParam` por domínio, UI em `src/ui`, `TargetedBlock` autoritativo, sistemas change-driven, `DeduplicatedQueue<T>`/`VoxelUpdateQueue`, `FrameWorkBudget`, evitar scans globais sem orçamento. Não criar nova arquitetura sem necessidade reproduzida. Preservar assets originais, não gerar imagens sem pedido; não prometer execução assíncrona.

## Invariantes de runtime e pipeline

Pipeline: streaming visible/preload → geração async → integração/seed luz por residência (.38) → snapshot halo 3×3×3 → mesh async → render → archive/unload. Resultados revisionados; desatualizados são reenfileirados, geometria/fluidos/luz separados. Luz em lote (.41), unload halo 26 vizinhos (.40), HDR world order0 Skip/viewmodel order1 tonemap/UI SDR order2. Hidrologia: endpoints XYZ compartilhados e margem global (.12), suporte físico por aresta antes de ranking (.48), nunca permitir água flutuante; água natural pertence à geração, não deve entrar em massa no solver de fluido dinâmico. `BiomeField` determina bioma autoritativo, `GenerationColumnSample` altura original, densidade antes de material e fluidos. Sol/lua seguem céu/relógio, nuvens world-space; `celestial.rs` usa camera_position+offset e usuário relata lua seguir câmera. Held block acompanha hotbar; R apenas placement.

## Estado verificado 16/09/2026 — develop v0.15.51

**`VERSION`: `0.15.51`**, commit `966989937b5b80fe3414481a6bc58c2c191b9c1c`. Último commit de código anterior à consolidação: **`e80243f39ce7b81e4a031a71d02109c855442072`**. Handoff base `43f33c305a3e6428de7e5291b2110c1b9662f2fc`. Esta atualização é DOCUMENTAL; nenhum código, configuração de CI ou VERSION de feature foi mesclado. Consultar branch antes do próximo bloco.

**CI .50 final-code sucesso**: push `35155018897`, SHA `599d76791d6048f887d7c69d842645c74b74de1f`, Clippy e Check success; workflow antigo NÃO executava asserts. Também sucesso bump .50 `35154962840`, .49 `35154000629` e .48 `35153673196`. **CI .51 código final**: push `35156113558`, SHA `e80243f`, na última consulta job em progresso, **Clippy e Check success, Tests in_progress sem conclusão**. A CI intermediária bump `35156058752` não inclui ajuste de visibilidade `e80243f`; a do código final tem precedência. Não alegar sucesso integral de .51 antes do resultado da etapa Tests.

### .51 — densidade real e água na costura das regiões

- `src/world/generation/fluids.rs`, commit `208bbbbb2717e4aca50bc9b52264d470d167b34c`: substituiu vetor de densidade inteiramente vazio da .50 pela chamada **real** `sample_density_field`, com `DensityPassContext`, `VolumeBiomeRegion::default`, `BiomeField` construído da definição REAL de Plains e da dimensão Overworld filtrada apenas para Plains, com seaLevel 64 compatível. `GenerationColumnSample` mantém altura original plana 104 e influência Plains 100%; o gerador de rios usa declive macro sintético e seed 42. Seleção da travessia exige River fisicamente suportado nos dois lados, voxel molhado e **abaixo da superfície original**, garantindo `terrain_density` inicial positivo. Para cada chunk x=7/8, verifica densidade final não sólida e usa `rasterize_fluid_pass` para exigir fonte cheia em ambas as faces.
- A busca .50 examinava Z de -384 a +384 nos grafos das regiões `(0,0)` e `(1,0)`, embora muitos Z pertençam a outras regiões. Agora constrói pares de regiões corretos `(0, region_z)` e `(1, region_z)` para `region_z` em `-2..=2`, amostra apenas cada faixa de 128 blocos e preserva negativos com `div_euclid`/`rem_euclid`. Se a fixture não encontrar travessia, o teste falha explicitamente: investigar fixture/seleção antes de afirmar que há bug no jogo.
- `src/world/hydrology/region.rs`, commit `e80243f39ce7b81e4a031a71d02109c855442072`: coord de `HydrologyRegion` tornou-se `pub(crate)` para verificar `GenerationRegion.coord.xz() == hydrology.coord` no teste; não duplica estado.
- `.github/workflows/ci.yml`, commit `6ca679d47181d01f26a7be7b86c4f211d775b802`: adicionou `cargo test --bin asteria -- --test-threads=2`, antes só Clippy e Check. Bump `.51` commit `966989937b5b80fe3414481a6bc58c2c191b9c1c`.
- **Limitações**: fixture tem coluna de altura e terreno macro sintéticos, Plains isolado, sem cavernas, sem rasterização de materiais/structures, sem mundo de jogo completo. Usa as funções reais de densidade e colocação de fluidos. Só execução da CI comprova asserts. Teste único de costura não prova rios em 2000 blocos, destino oceânico, múltiplas seeds, FPS ou ausência de chunks pretos.

### Trabalho anterior preservado

`.50`: `77f0b67`/`e0dbf7f`/`599d767` teste gerador→rasterização com vetor todo vazio, bump `0a10707`, handoff final `b12e4dd`; CI final-code success `35155018897`. `.49`: `4e733ea` regressão grafo/água/densidade em regiões adjacentes, bump `497e1ff`, CI success. `.48`: `feature_graph.rs` `3e96e19`, `region/water.rs` `eefb498`, `region/density.rs` `3ea0f38`, filtro por aresta suportada antes de escolher mais forte, CI success. `.47`: cache de drenagem não memoriza falso negativo inconclusivo de trace limitado a 64. `.46`: oceano usa altura original na amostragem física. `.45`: suporte consistente carving/água. Histórico completo nos links acima. Não confundir CI check com teste executado.

## Consolidação dos handoffs dos branches de feature — documental, NÃO código mesclado

**Proveniência conferida em 16/09/2026:** [handoff criaturas, branch `feature/slime-creature-integration`, SHA `ba80f79`](https://github.com/sarakborges/mineclone/blob/ba80f79bc263e64c4f5a34181f1d7b32521c67af/HANDOFF.md); [handoff chat atual, branch `feature/game-chat-creature-command`](https://github.com/sarakborges/mineclone/blob/feature/game-chat-creature-command/HANDOFF.md); [handoff `feature/native-input-diagnostics`](https://github.com/sarakborges/mineclone/blob/feature/native-input-diagnostics/HANDOFF.md). O terceiro contém histórico amplamente sobreposto ao chat e divergiu do seu HEAD; **não** interpretar como já integrado nem copiar seu estado de CI como se fosse da branch final. Vários outros branches enumerados pelo GitHub não possuem `HANDOFF.md` na raiz (incluindo `feature/block-optical-properties`, `feature/voxel-lighting-v2`, `feature/directional-sun-shadows`, `feature/held-dynamic-light`, `feature/lamp-block`, `debug/render-oom-diagnostics` e `fix/swimming-worldgen-connectivity`); não inventar entregas/validações para eles. Esta consolidação só incorpora o conteúdo documental encontrado, preservando seu contexto e data.

**Matriz de linhas de trabalho (não somar versões):** `develop` v0.15.51 (código de mundo/hidrologia e CI nova, base desta documentação); `feature/slime-creature-integration` v0.16.0, PR [#9](https://github.com/sarakborges/mineclone/pull/9) OPEN/DRAFT direcionada a `develop`, HEAD `ba80f79bc263e64c4f5a34181f1d7b32521c67af`; `feature/game-chat-creature-command` v0.17.3, PR [#10](https://github.com/sarakborges/mineclone/pull/10) OPEN/DRAFT direcionada a `feature/slime-creature-integration`, dependência em cadeia; `feature/native-input-diagnostics` v0.17.3 é ramo experimental divergente com handoff repetido e workflows/scripts diagnósticos, não PR de integração presumida. A referência `develop=0.15.41` no handoff de chat descreve sua base HISTÓRICA ao iniciar a feature, não o estado atual `.51`.

### Criaturas — v0.16.0 / PR #9 (pendente de integração)

- Sistema de criaturas orientado a dados: definições em `data/creatures/**/*.json`, `id` estável e nome localizado `LocalizedText` com inglês obrigatório; modelos GLB/glTF em `assets/models/creatures/`, caminho relativo a `assets/` declarado no JSON. Modelos podem ser reutilizados por espécies, com tints HSI por instância, slots de materiais/animações nomeados, parâmetros de pulo e colisão AABB no ECS root sem deformação pelas animações.
- Meadow Slime e Ember Slime compartilham o `assets/models/creatures/slime/slime.glb` original, com gerador e metadata `slime.collider.json`; handoff/PR registram arquivo binário efetivamente commitado com SHA-256 verificado na criação. Spawn de preview é opt-in e locomoção física simples; NÃO existe ainda spawn definitivo por bioma, IA final, combate, persistência, rede ou interface completa de mods.
- PR #9 permanece DRAFT e não mesclada, mergeability reportada como `false` no snapshot consultado; reavaliar conflito contra `develop` atual antes de integrar. A feature nasceu quando `develop` estava na `.40/.41` e não incorpora automaticamente correções de iluminação `.41` nem hidrologia `.42–.51` ou nova CI; rebasing/merge requer comparação de código e testes por SHA. Testar GLB, collider, animações, cores, salto, carregamento real e FPS no jogo. Não afirmar sucesso de CI desta feature a partir de CI da `develop`.

### Chat, comando de criatura e scrollbars — v0.17.3 / PR #10 (pendente)

- HUD de chat translúcida à esquerda acima do player HUD (`left=18`, `bottom=138`). `T` abre/foca sem inserir o T, `Enter` envia/fecha e `Esc` fecha sem pausar; texto até 256 caracteres. Texto comum usa nome provisório `Yogg'Sara`. Comandos `/` usam parser; `/spawn_creature <id>` consulta somente `CreatureRegistry` data-driven, tenta coordenadas exatas do jogador (pé = câmera menos altura dos olhos), valida chunk carregado e AABB desocupada, retorna erro/sucesso no chat; Meadow e Ember precisam de teste no jogo. Isso NÃO inclui spawn natural, rede ou persistência.
- Histórico `VecDeque` cronológico (antigo acima/novo abaixo, últimas 64 mensagens), painel ancorado embaixo que cresce PARA CIMA, altura **máxima** de 15 linhas VISUAIS com wrap (`15 * 22px`), scroll vertical e autoscroll com medida em `Last`, mouse wheel, ocultação dez segundos após última mensagem com chat fechado. Scrollbars condicionais de inventário, Settings e New World usam `src/ui/scrollbar.rs`/`AutoScrollbar`; mostrar só se `content_size.y > size.y + 0.5`, esconder se sem overflow. Helper `persistent_vertical_scrollbar` morto foi removido, não silenciar warnings.
- Histórico de validação: CI anterior ao editor nativo [35152201657](https://github.com/sarakborges/mineclone/actions/runs/35152201657) success Clippy/Check; PR bot-authored anterior ficou `action_required` sem jobs, não inferir erro do código. Checklist manual em [`docs/chat-and-scroll-qa.md`](https://github.com/sarakborges/mineclone/blob/feature/game-chat-creature-command/docs/chat-and-scroll-qa.md). PR #10 ainda DRAFT, sem confirmação visual/gameplay de janela, scroll e spawn.

### Editor nativo de texto — correção v0.17.3 isolada

- Correção de regressão de inputs manuais: chat, busca no inventário, busca de biomas, seed de novo mundo e ticks/segundo migrados para `EditableText` do Bevy 0.19.1, com `bevy/system_clipboard` para seleção, caret, arraste, teclado, Ctrl/Cmd+A/C/V/X, Unicode e IME; sistemas do jogo mantêm foco, submit/Esc e validação numérica. Limites: chat 256, busca de biomas 128, seed 20 dígitos, tick rate 10 dígitos. Sem cache próprio de clipboard; não trocar o editor por parsing manual sem reproduzir regressão. Checar comportamento real do SO, não só compilação.
- Handoff atual do chat registra patch completo com Clippy estrito e `cargo check` **success** no workflow [35155837628](https://github.com/sarakborges/mineclone/actions/runs/35155837628), publicação do commit `86da47735fc64541d45a1b8218b5d58a3a29c4ef` no workflow [35156635406](https://github.com/sarakborges/mineclone/actions/runs/35156635406) e limpeza subsequente de formatação incidental; testes unitários não foram gate por pedido específico dessa feature e **não estão confirmados**, runtime tampouco. A CI de `develop` v.51 com `cargo test` não retrovalida os SHAs separados do chat.
- O ramo `feature/native-input-diagnostics` contém handoff da mesma história, mas divergiu: tem workflows `inspect-native-input.yml`/`validate-native-input.yml` e scripts diagnósticos próprios. Tratar apenas como referência isolada; não trazer scripts/workflows experimentais nem duplicar entradas na linha de `develop` sem inspeção e autorização para integração de código.

### Integração futura das features — NÃO realizada nesta consolidação

1. Partir de `develop` atual, comparar mudanças da PR #9 e suas dependências contra `.41–.51`, reconciliar conflitos sem perder correções de luz, rios, streaming e CI/testes; validar build/Clippy/testes/artefatos e gameplay das criaturas. Não mesclar PR #9 DRAFT apenas porque o handoff foi reunido.
2. Só então reavaliar a PR #10 empilhada na #9; preservar chat, `/spawn_creature`, editor nativo e scrollbars, conferir que o código final do branch (não scripts diagnósticos) corresponde à v0.17.3 e testar inserção, seleção, clipboard, foco, pause/input, scroll e render/GLB em Windows. Validar versão SemVer na linha realmente integrada; **não** substituir cegamente `VERSION` de `develop` por `.17.3` nem somar incrementos.
3. Documentar CI de cada SHA, divergências e conflitos reais. Features isoladas não encerram os nove bugs de mundo e a CI nova da `develop` não valida versões antigas. Manter os PRs DRAFT enquanto houver bloqueios declarados.

## Nove bugs do usuário — TODOS ABERTOS até confirmação em runtime

| # | Prio | Sintoma e lacuna |
|---|---|---|
| 1 | P0 | Chunks pretos/costuras iluminação e AO. Mecanismos `.33/.38/.39/.40/.41` parciais; medir transiente/persistência e FPS. Features de luz em outros branches não estão integradas. |
| 2 | P0 | Rios terminam no nada. `.42–.48` seleção/carving, `.49` costura, `.50–.51` regressões de água+densidade; falta asserts .51, múltiplas seeds, destino até oceano e jogo. |
| 3 | P0 | Percurso de 2000 blocos só Plains/Mountains. `.35` belt estreito não confirmado; medir `sample_surface().primary_id` por seed/percurso e comparar aparência, sem alterar pesos às cegas. |
| 4 | P0 | Margem afunda antes da água. `.36/.37/.43–.48` parciais, preservar margem global `.12` e suporte físico. |
| 5 | P1 | Confluência rio–lago abrupta: conferir XY/largura/profundidade/nível e endpoints. |
| 6 | P1 | Aberturas de túneis com paredes retas e interseção com rios. |
| 7 | P1 | Lua segue câmera: `celestial.rs` usa `camera_position + offset`; não mudar órbita às cegas. |
| 8 | P1 | Lua visível de dia: `.32` setPhase Night não confirmado visualmente. |
| 9 | P1 | Highlight ignora camadas: `targeting/highlight.rs` cubo branco scale1.01/AlphaMode Blend; checar bounds/overlay. |

Outros pontos não fechados: fogColor artístico; FPS baixo no streaming/caminhada sem benchmark; R/held `.18` não validados. HUD planejado: avatar placeholder `Yogg'Sara` inferior esquerdo, `50 / 100` vida, status acima; target acima mira, ícone slot à esquerda e texto com sombra à direita. Único bug visual explicitamente confirmado resolvido pelo usuário: céu por bioma `.24`. Nenhuma feature em branch isolada altera esse status.

## Roadmap de execução — próximos blocos

1. **CI .51**: ler steps/logs do run push `35156113558`; Clippy e Check success na consulta desta consolidação, `Tests` ainda in_progress. Se `cargo test` falhar, corrigir causa sem apagar regressão ou inventar travessia; registrar quantos testes passaram/falharam e custos de CI.
2. **Rios P0**: adicionar cenário determinístico que prova conexão fonte→destino oceânico sobre mais de uma região e leito físico não suspenso; analisar cap 64 em `river/selection.rs` em cenário reproduzível e medir impacto antes de aumentar. Expandir fixture para mais seeds e terreno realmente gerado pelo `BiomeField`, sem falsear altura ou buracos.
3. **Biomas P0**: testar/amostrar `BiomeField::sample_surface().primary_id` em trajeto de 2000 blocos e múltiplas seeds, usando dimensão/biomas reais; distinguir ausência na seleção de material/visual. `selection.rs` exclui bioma dominante quando 5/8 vizinhos e aplica `avoidNear`; `surface.rs` mistura montanhas; não alterar pesos sem evidência.
4. **Luz P0**: examinar `chunk_remesh.rs`, `chunk_remesh_tasks.rs`, `voxel/mesh_snapshot.rs`, `lighting_updates.rs`, reproduzir estado preto por halo/relaxamento; seed/catchup e 26 vizinhos de `streaming.rs` foram auditados na .50, não provam resolução. Conferir também margem de água P0 e depois P1 confluências, túneis, lua e highlight.
5. **Features empilhadas separadas**: roteiro na seção de integração PR #9 → PR #10; comparar SHAs e regras de CI atuais, sem transportar automaticamente políticas locais de branch para o roadmap geral. Cada bloco de código: arquivos/HEAD → reproduzir/testar → correção fundamentada → bump `VERSION` → HANDOFF com status real.
