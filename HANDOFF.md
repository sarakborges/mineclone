# HANDOFF — Asteria / Mineclone

**Fonte canônica:** `sarakborges/mineclone`, branch `develop`, Rust + Bevy 0.19.1. Versão raiz `VERSION`: **0.20.4** (patch visual do Brush, sem encerrar o trabalho de save). `Cargo.toml` `0.10.16` tem versionamento independente. Este checkpoint integra a PR [#13](https://github.com/sarakborges/mineclone/pull/13), a pedido explícito do usuário, com o trabalho da etapa 54 que avançou em `develop` durante a correção. **A versão ampliada/pivô do Brush não teve QA visual Windows nem CI específica confirmada antes da integração**; a CI do Brush anterior ao ajuste passou no [run 35257944441](https://github.com/sarakborges/mineclone/actions/runs/35257944441). A etapa 54 teve CI verde no [run 35258727224](https://github.com/sarakborges/mineclone/actions/runs/35258727224). Não tratar resultados anteriores como validação da árvore integrada.

## Histórico integral obrigatório

- [Etapas 0–12](docs/handoffs/auditoria-0-12-2026-09-17.md), [13–16](docs/handoffs/auditoria-13-16-2026-09-17.md), [17–20](docs/handoffs/auditoria-17-20-2026-09-17.md), [21–24](docs/handoffs/auditoria-21-24-2026-09-17.md), [25–27](docs/handoffs/auditoria-25-27-2026-09-17.md), [28–31](docs/handoffs/auditoria-28-31-2026-09-17.md), [32–36](docs/handoffs/auditoria-32-36-2026-09-17.md), [37–44](docs/handoffs/auditoria-37-44-2026-09-17.md) e [45–53](docs/handoffs/auditoria-45-53-2026-09-17.md). Ler também `ARCHITECTURE.md`.
- [Handoff integral de develop até a etapa 54](docs/handoffs/handoff-develop-pre-brush-merge-2026-09-17.md), blob `27b903e2b8d68d5603e379dc20b909a9ef8eefa0`, preservado sem cortes antes de resolver conflito.
- [Handoff integral da branch Brush antes do merge](docs/handoffs/handoff-brush-pre-merge-2026-09-17.md), blob `533d6565f2044697fe70ab8135a0852dbd97ca1a`, preservado sem cortes, inclusive tentativas e ressalvas.
- [Handoff ainda anterior à branch Brush](docs/handoffs/handoff-pre-brush-2026-09-17.md), blob `49d20d15d857118bd660a0d619ef58a9cf37d3ab`.
- [Handoff integral da branch Chisel antes do merge](docs/handoffs/handoff-chisel-pre-merge-2026-09-17.md), blob `b2a6f4416fe6ae07998a0dac7416778502833e22`, preservado na integração da PR #12. Detalhes funcionais em [docs/chisel-microblocking.md](docs/chisel-microblocking.md).

**Regras permanentes:** atualizar este handoff a cada checkpoint com commits, CI, limites e próximos passos, arquivando a versão integral antes de condensar. Bump semver por bloco funcional, não por documentação; save só se encerra após QA real. Corrigir warnings sem supressão. CI exige auditoria PT-BR/espanhol/inglês, `cargo clippy --locked --all-targets --all-features -- -D warnings` e `cargo check --locked`; não executar/adicionar `cargo test` sem autorização expressa. Não alterar PNG/GLB autorais. Pause não congela mundo, fluidos ou relógio. `Cargo.lock` gitignored, cache CI deve ser preservado. Não inventar execução, Windows, métricas ou sucesso de QA.

## Estado de save — etapas 0–54 ainda em aberto

Persistência de mundos isolados com snapshots/manifests imutáveis, chunks alterados residentes e arquivados, inventário, posição, regras, dimensão e relógio; autosave de alterações, Leave/Exit após publicação confirmada, fallback verificando geração completa e quatro backups recuperáveis. Scan/load/prune em workers, leases por mundo, descarte de carga cancelada fora do frame, telemetria de operações da main thread e workers. Etapas 51–53: rejeição antecipada de coordenadas duplicadas, nomes reservados Win32 adicionais e sufixos de nomes com 200 unidades UTF-16. Etapa 54 adicionou `tools/make_save_fixture.py` com seis modos de corrupção em cópias isoladas e atualizou `docs/save-roundtrip-qa.md`; verificação Python sintética registrada no arquivo integral da etapa 54, **jogo Rust e QA Windows NÃO EXECUTADOS**. Corrida em `create_new_world` após `create_dir AlreadyExists` para nomes longos ainda pendente. Não remover, reverter ou pressupor validação desses trabalhos ao integrar Brush.

## Brush — PR #13, correção visual integrada por solicitação do usuário; QA pendente

- Branch `fix/brush-hand-render-layer-order`, criada de `develop` em `c087f09b5d06a00ddbfebf5e0fbb18359d9995e3`. A primeira tentativa `c78adff2` diminuiu indevidamente `0.43` para `0.30`; usuário esclareceu que precisava **aumentar o sprite e colocar o cabo na mão**. A correção `89965d758e7c454037a162fb0bfccac49dd8a8ba` usa tamanho `0.52`, root da pegada `(-0.08, 0.46, 0.21)` no frame da mão, offset de extremidade `(-0.36, -0.36)` relativo ao tamanho e rotação local Z `+0.30` rad em torno do cabo.
- Arte-base usa `AlphaMode::Mask(0.5)` em vez de dois materiais `Blend` concorrentes; tinta permanece `Blend`, com separação Z `+0.004`. Arte-base e tinta compartilham a mesma pose e seguem cor/visibilidade da paleta. PNGs autorais, hotbar e formato de save intactos.
- `VERSION` `0.20.3` → `0.20.4` no commit `457eec2`, mantida após corrigir escala/pivô. A fusão exigiu resolver conflito de `HANDOFF.md` porque develop avançou até a etapa 54; ambos os textos integrais foram arquivados acima e esta fonte consolida os dois trabalhos sem reverter código da etapa 54. A PR foi marcada pronta e o usuário pediu merge mesmo sem QA visual.
- **QA do Brush: NOT RUN.** Validar no Windows sprite maior, extremidade do cabo dentro da mão, cabeça não cortada, animações de troca/quebrar/colocar, ausência de z-fighting e comparação sem tinta/com duas tintas contra hotbar. CI correspondente ao head integrado deve ser verificada separadamente; `cargo test` não autorizado.

## Chisel — PR #12, integração solicitada; validação runtime pendente

- Branch `feature/chisel-microblocking`, originada de `develop` em `f06c68c39563b07095f383b060639463b0aac659`. Usuário autorizou integração à `develop` mesmo com QA de gameplay não realizado. Merge conciliou a evolução paralela da etapa 54 e a PR #13 (Brush); preservar versão atual **0.20.4** e o código de saves/Brush, sem regressões ou bump especulativo. Snapshot integral da branch arquivado acima; especificação detalhada em `docs/chisel-microblocking.md`.
- Três resoluções: **Thick** (2×2×2), **Thin** (4×4×4) e **Extra Thin** (8×8×8), com R alternando. Full removido por solicitação; clique esquerdo remove fragmento, direito coloca. Material, textura, rotação e propriedades visuais herdados do bloco macro; tronco oco e placa de espessura 1/8 são cenários de QA. O campo `tags` em definições habilita opt-in `fragmentable`; pedra, terra, grama, areia, basalto e tronco permitidos; `oak_leaf`, `lamp`, `glass` bloqueados. Tooltip usa o `ActionHint` do Brush junto da mira, não hotbar; HUD EN/PT-BR/ES.
- Código inclui máscara de ocupação, seleção, raycast/colisão subvoxel, faces agrupadas, UV e preview. Persistência da forma **intencionalmente desabilitada** nesta primeira etapa: blocos macro originais são serializados sem microgeometria; peças criadas no vazio não devem ressurgir como cubos completos. No conflito `src/voxel/chunk_disk.rs`, manter otimização de validação da etapa 54 sem `HashSet` e adicionar filtro de pais transitórios do Chisel.
- CI verde anterior à etapa final da branch em `e7f4911829e07b988d1a3dda699fba54956cde4f`, [run 35257019737](https://github.com/sarakborges/mineclone/actions/runs/35257019737). **Não confundir com validação da integração atual.** Windows gameplay QA, medição FPS, lighting/fluidos em cavidades e controle de memória do interner de máscaras pendentes. `cargo test` não executado/autorizado.

## Próximos passos

1. Acompanhar CI do merge/árvore integrada, corrigir erros/warnings sem supressão; registrar resultado neste handoff, sem confundir runs de commits anteriores.
2. Fazer QA visual Windows do Brush conforme critérios acima; se necessário ajustar somente tamanho/posição/pivô e não voltar a reduzir a textura.
3. Executar `docs/save-roundtrip-qa.md` em Windows usando cópias descartáveis: nomes longos/reservados, snapshots corrompidos/fallback, chunks/fluidos/brush, posição/inventário/regras/relógio, autosave/Leave/Exit, poda, Back, RAM/CPU e logs de captura/publicação; registrar PASS/FAIL/NOT RUN. Avaliar semântica exploratória de `player-null` antes de alterar formato.
4. Corrigir corrida de nomes longos em `save_catalog.rs::create_new_world` no ramo `AlreadyExists` sem ultrapassar 200 UTF-16. Examinar concorrência scan/load/prune e stalls usando medições reais, sem sacrificar backups.
5. Fazer QA do Chisel no Windows: seleção e tooltip do Brush, três precisões, bloqueio por `fragmentable`, buracos/colisão, textura e UV do tronco, transparência, chunk crossing/negative coordinates, streaming durante a sessão e reinício sem salvar microforma. Investigar iluminação/fluidos macro e substituir máscara internada por overlay dedicado antes de afirmar desempenho estável.
