# HANDOFF — Asteria / Mineclone

**Fonte canônica:** repositório `sarakborges/mineclone`, branch `develop`, Rust + Bevy 0.19.1. **VERSION raiz: `0.20.3`** (bloco funcional aberto por falta de QA em jogo); `Cargo.toml` usa `0.10.16` independentemente. **Último código validado pela CI:** [`55b8b3a`](https://github.com/sarakborges/mineclone/commit/55b8b3a6dad389de25c7ab879009df246facdb94), [CI PR `35175024558`](https://github.com/sarakborges/mineclone/actions/runs/35175024558), job `105054715461`: `cargo clippy --all-targets --all-features -- -D warnings` e `cargo check` concluíram com sucesso. NUNCA inferir disso que visual, física, Windows, render ou QA de gameplay estão validados.

## Histórico integral e regras permanentes

- [Handoff integral imediatamente anterior a esta série](https://github.com/sarakborges/mineclone/blob/7a2e849523f5dfb0b72ddea1a1b5a02b2d0ce655/HANDOFF.md): todo o contexto de migração de estruturas, cache, colisões, slimes, `/spawn`/`/place`, inputs e CI anterior; **permanece vinculante onde não atualizado abaixo**. [Handoff ainda anterior `fafba08e`](https://github.com/sarakborges/mineclone/blob/fafba08e92c57a883a689cb694ead6b6e42116d5/HANDOFF.md), [detalhado `500663e`](https://github.com/sarakborges/mineclone/blob/500663ea569e0a46361692623a5a3fa68d8042ad/HANDOFF.md) e outros históricos ali encadeados. Nunca descartar conteúdo histórico ou requisitos sem evidência.
- Em `go`/`continua`, conferir HEAD, `VERSION`, CI, código e HANDOFF, agir no `develop`, **dar feedback frequente, concreto e consolidado**, sem silêncio prolongado ou promessas vagas. Usuário explicitamente reclamou da falta de feedback no meio da série atual. Não inventar validação.
- A cada bloco funcional relevante **concluído**, incrementar `VERSION` SemVer (minor para features, patch para correções), atualizar HANDOFF com commits/resultados/pendências. Alterações de documentação ou CI isoladas não exigem bump. O bloco atual ainda depende de QA real, portanto `VERSION` não foi incrementada; próximo minor previsto de `0.20.3` é `0.21.0` quando validado.
- CI contém exclusivamente `cargo clippy --all-targets --all-features -- -D warnings` e `cargo check`. Corrigir TODOS erros e warnings sem silenciar via `#[allow]`. **Não rodar nem acrescentar `cargo test` à CI sem pedido explícito.** Não ficar repetindo aviso sobre cargo local. CI não valida UI, gameplay ou Windows.
- Arte: cubos e bordas retas, sem bevel/smoothing; rostos dos slimes nos PNG externos 64×64 de cada espécie, nearest; não impor formato 64×64 a outras criaturas; preservar Bassalt, Brush e os assets do usuário. Respeitar `ARCHITECTURE.md`.
- Estruturas apenas em `data/structures/` e referências por ID, loader rejeita arquivos de estrutura fora da pasta global.

## Solicitações e implementação da série atual

**Pedidos do usuário:** remover chevron do autocomplete na opção selecionada; substituir as instruções curtas `Select/Tab/Esc` por texto detalhado coerente com os demais controles; corrigir espaçamento/alinhamento vertical de inputs; renomear IDs das boulders e árvore para `boulder_{size}` e `tree_oak`; fazer `/place` encaixar no terreno já gerado usando a lógica da geração natural (sem pairar); `/spawn` criar criatura no ar e ela cair, impedir criação dentro de blocos; Brush selecionado não mostrar dicas genéricas Break/Place; dar prioridade de target a criatura com HUD de entidade e esconder HUD de bloco; **HUD de entidade deve ter apenas nome localizado e quadrinho com miniatura da criatura real, sem ID nem coordenadas**; **Brush deve aparecer na mão do personagem ao equipar**.

- Autocomplete: chevron removido [`949a7da`](https://github.com/sarakborges/mineclone/commit/949a7dadad53fcf096f6b09bca2e15bb0dcf0196); instruções ampliadas [`42021a6`](https://github.com/sarakborges/mineclone/commit/42021a61eafba956559b8465b7860de745defbf6).
- Inputs: ajustes das caixas internas de texto no chat, inventário, pesquisa de biomas e inputs numéricos para centralização vertical: [`ee0bf47`](https://github.com/sarakborges/mineclone/commit/ee0bf4745b8823fe4f452e0a64d91f0901d98670), [`b029dd4`](https://github.com/sarakborges/mineclone/commit/b029dd4017d15e6a57a82084702e6e1a4741d8c7), [`4acb6b1`](https://github.com/sarakborges/mineclone/commit/4acb6b1afd843cdf8157fff7be744003c0486763). Compilação aprovada, **QA de layout/caret/IME/clipping e múltiplas resoluções pendente**.
- Nomes de estruturas: [`44823c2`](https://github.com/sarakborges/mineclone/commit/44823c2ecae2eeb2826c6401acd2afce657e639a) renomeou atomicamente JSONs, IDs e referências em biomas para `asteria:boulder_small`, `asteria:boulder_medium`, `asteria:boulder_big`, `asteria:boulder_huge`, `asteria:tree_oak`, preservando `stone_marker`.
- Target: [`2b6cbdb`](https://github.com/sarakborges/mineclone/commit/2b6cbdb9ca6d97ed821b5c8922fde4f09a45794b) usa raycast sobre blocos e AABBs de criaturas em raio de oito blocos, recurso `TargetedCreature` é exclusivo de `TargetedBlock`; evita HUD, outline, preview e ação de bloco enquanto entidade é alvo. HUD independente registrado via [`22541be`](https://github.com/sarakborges/mineclone/commit/22541bef4a9b4ba03ba1a406376d061945e629a7), [`3430c54`](https://github.com/sarakborges/mineclone/commit/3430c546d24aac3baec67461e203e95a2c5f6965) e refinado em [`48981b5`](https://github.com/sarakborges/mineclone/commit/48981b5bba9c9945a98c3f37a232652dbecd05ee). Brush esconde dicas genéricas Break/Place e mantém dica específica de tintura via [`568e1f9`](https://github.com/sarakborges/mineclone/commit/568e1f9045e8963029d5022a72020f10b4550753).
- **HUD de criatura — correção posterior específica:** `src/hud/entity_targeting.rs` [`d49a15e`](https://github.com/sarakborges/mineclone/commit/d49a15e559bf0a874b2eb615fcba4f2d5e01c7ad) mudou de texto com nome/ID/coordenadas para somente nome localizado, ao lado do mesmo quadrinho 44×44 e imagem 34×34 do HUD de bloco. `src/hud/entity_targeting/portrait.rs` [`be6d29b`](https://github.com/sarakborges/mineclone/commit/be6d29b0e25eb05f1aa631833b97cb6efddc93ed) renderiza modelo GLTF real da definição numa câmera isolada RenderLayer 2 para textura RGBA 128×128 usada pelo quadrinho, aplicando as mesmas tinturas e texturas da espécie; a câmera só ativa com target válido. Ajustes de imports, luz compatível Bevy, cleanup e visibilidade Rust em [`6e815fe`](https://github.com/sarakborges/mineclone/commit/6e815fe2483baf6548d5a801e9db8f68b892cb0b), [`7848f68`](https://github.com/sarakborges/mineclone/commit/7848f68cbfafe5ab72c830046493fbf639b79d93), [`22a1f93`](https://github.com/sarakborges/mineclone/commit/22a1f9311c36b376d89da6f7def43426fe1e3e1b). **Necessário testar enquadramento do GLTF, alpha, texturas, performance e ciclo de vida dentro do jogo; CI não valida imagem.**
- **Brush na mão — correção posterior específica:** `src/player/viewmodel/held_brush.rs` em [`f6256ce`](https://github.com/sarakborges/mineclone/commit/f6256ce874724a5a892d4175d3231872538f95e9) cria sprite 3D plano com textura original `textures/tools/brush.png` e overlay `textures/tools/brush-tint.png` condicionado à tintura, usando RenderLayer 1 e `NotShadowCaster`; anexa à entidade `PlayerViewModel`, portanto acompanha animação do braço. Registro de sistemas e setup em [`55b028d`](https://github.com/sarakborges/mineclone/commit/55b028d7242851e73c137c2aabd460de94ada372); `sync_held_brush` mostra somente no slot Brush, oculta ao trocar item, acompanha alteração de cor da paleta. Correções de mutabilidade/import e de tipos/alias Clippy [`38bc911`](https://github.com/sarakborges/mineclone/commit/38bc9115a4f6864fc519a7f9483278bb9b9a3568), [`55b8b3a`](https://github.com/sarakborges/mineclone/commit/55b8b3a6dad389de25c7ab879009df246facdb94). **Validar ingame posição, frente-verso e transparência da textura, trocas de item e overlay com e sem dye.**
- `/place`: função comum `fit_structure_to_ground` em `src/world/generation/structures/support.rs` reaproveita mesma regra de footprint inferior e tolerância de 1 bloco da worldgen: [`c56d41d`](https://github.com/sarakborges/mineclone/commit/c56d41d19e3bcfacbb02e3e077fc2506e895c173), [`5095d00`](https://github.com/sarakborges/mineclone/commit/5095d004523b59bc485a8191c3e76ac7e0a4a316), [`36ff0b2`](https://github.com/sarakborges/mineclone/commit/36ff0b297db1f7b48d55460ebf174c9bd762aa1a), [`9107571`](https://github.com/sarakborges/mineclone/commit/910757158fc1b59648ea51ee5f28cebba84dda96). `src/hud/chat/placement.rs` em [`aab42f8`](https://github.com/sarakborges/mineclone/commit/aab42f838e13ad20c36c6fd36355ae9c6db1c629) lê a superfície do `VoxelWorld` carregado/editado em cada coluna do footprint, posiciona sobre terreno sólido, requer todas as células destino livres/secas/carregadas, base de toda a camada inferior sustentada e evita entidades/reservas; falha sem editar se não encaixar. Só desloca player se realmente cruzar volume.
- `/spawn` no mesmo commit `aab42f8`: não exige chão nem ausência de fluido, porém exige collider em chunks carregados sem blocos sólidos nem overlap de criaturas; realocação do player não exige chão nesse comando. Movimento de criatura já possui gravidade e transição para airborne em `src/creatures/motion.rs`; verificar queda ingame. Não confundir exigência de ausência de fluido com `/place`, onde permanece.

## CI — evidências e pendências

- Antes das duas correções visuais recentes, [run `35174211051`](https://github.com/sarakborges/mineclone/actions/runs/35174211051) passou integralmente em `48981b5`. Entre novas alterações, [run `35174696642`](https://github.com/sarakborges/mineclone/actions/runs/35174696642) apontou imports unused no portrait e Brush, campo obsoleto PointLight e mutabilidade do material; corrigidos sem `#[allow]`. [run `35174834948`](https://github.com/sarakborges/mineclone/actions/runs/35174834948) apontou tipos privados em system params e `clippy::type_complexity`; resolvidos com visibilidade `pub(super)` e alias, commits `22a1f93` e `55b8b3a`.
- **Resultado final confirmado:** [run PR `35175024558`](https://github.com/sarakborges/mineclone/actions/runs/35175024558) do commit `55b8b3a`, job `105054715461`, `completed/success`. `Clippy`, `Check`, caches Linux e Cargo, post-cache todos sucesso. Não atribuir status a uma CI push adicional sem consultá-la.
- Infra/cache anterior: [`8503bac`](https://github.com/sarakborges/mineclone/commit/8503bac97aa3c88d1204eb56f79a891a7ad2da7f) fixa Ubuntu 24.04, cache de bibliotecas Linux e `Swatinem/rust-cache@v2`, mantém Clippy e Check. `.gitignore` ainda ignora `Cargo.lock` (reprodutibilidade de dependências a avaliar separadamente).

## Itens ainda abertos

- **QA visual real do HUD da criatura com miniatura GLTF e do Brush na mão**: enquadramento, iluminação, alpha, visibilidade, seleção/deseleção, troca de espécies, performance e assets; CI garante código, não desenho.
- QA do `/place` em terreno irregular, árvores e todas boulders, fronteiras de chunk, vizinhança de fluidos, player próximo e sem edição parcial; QA `/spawn` no ar/água, queda, blocos próximos, múltiplas criaturas, sem colisões.
- QA do empurrão mútuo player–criatura (`src/player/movement/entity_collision.rs`) contra paredes e múltiplas criaturas; QA dos slimes/animações e inputs verticais/caret/IME.
- Só quando o bloco de features tiver QA e correções finais: incrementar VERSION conforme SemVer, registrar evidências e atualizar HANDOFF. Não encerrar prematuramente.
