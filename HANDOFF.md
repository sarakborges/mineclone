# HANDOFF — Asteria / Mineclone

**Fonte canônica:** `sarakborges/mineclone`, branch `develop`; Rust + Bevy 0.19.1. **Versão raiz na branch de UI `feat/ui-redesign-sidebar-pause`: `0.21.0`**; `develop` estava em `0.20.3` quando a branch foi criada, bloco de saves segue com QA pendente. `Cargo.toml`: `0.10.16`, independente. **Último código comprovadamente verde no início deste trabalho:** `eba1a7a2d7120f143ffec2577396d6091dc81de4`, https://github.com/sarakborges/mineclone/actions/runs/35234439682 — Clippy `--all-targets --all-features -- -D warnings` e `cargo check --locked` success. CI não equivale a QA Windows ou roundtrip real. Checkpoint UI mais recente abaixo.

## Histórico integral — consultar obrigatoriamente antes de alterar decisões

- [Etapas 0–12](docs/handoffs/auditoria-0-12-2026-09-17.md), blob `34161a1ec793041a738ec704e41dc345cc87fff6`.
- [Etapas 13–16](docs/handoffs/auditoria-13-16-2026-09-17.md), blob `cbbb6710e8be6b132467c943f03ed8df326509e3`.
- [Etapas 17–20](docs/handoffs/auditoria-17-20-2026-09-17.md), blob `bc4a5e7f5ff723cc6f52ef0c50edce73b37b5da2`.
- [Etapas 21–24](docs/handoffs/auditoria-21-24-2026-09-17.md), blob `8b35a7c3680f7df1d3aaf4e142fcf39ed605036d`.
- [Etapas 25–27 e requisitos expressos de save](docs/handoffs/auditoria-25-27-2026-09-17.md), blob `681c00fbb1674687affd4cf87d06c9317213c707`.
- [Etapas 28–31 integralmente](docs/handoffs/auditoria-28-31-2026-09-17.md), blob `9b46ae054ed205590d67a353fb5117c471e419a8`.
- [Etapas 32–36 integralmente e requisitos/cache CI](docs/handoffs/auditoria-32-36-2026-09-17.md), blob `33ea041165e699a5a95480f60f9b84a26d33d005`: cópia integral do handoff anterior, sem excluir história ou pendências. Ler também `ARCHITECTURE.md`.

**Regras permanentes:** efetuar commits coerentes na `develop` (o trabalho de UI pedido expressamente está isolado em feature branch até revisão); incrementar `VERSION` major/minor/patch ao concluir cada bloco funcional, nunca apenas por documentos e não encerrar bloco de saves sem QA. Atualizar este handoff após checkpoints com SHAs, CI, limites e próximos passos, arquivando íntegra antes de condensar. Corrigir erros e warnings sem supressão, Clippy rigoroso e cargo check, não adicionar/executar `cargo test` sem autorização expressa. Não alterar PNG/GLB autorais. Pause NÃO congela mundo/fluidos/horário. Cache efetivo de pacotes Linux, `Cargo.lock` restaurado antes de rust-cache e artefatos compilados entre commits; evitar reinstalar/recompilar Bevy sem necessidade. Não inventar QA ou execução do jogo; informar avanços concretos sem prometer trabalho posterior.

## Base de saves — etapas 28–36

UI de nomes exclusivos, reserva de diretório, manifesto e snapshots imutáveis (`worlds/{id}`, ignorado no git); chunks editados residentes/arquivados com IDs estáveis; inventário, posição, regras, dimensão, relógio e autosave condicional a cada 60s; Leave/Exit salvam antes de sair e erro mantém mundo carregado. Fallback tenta cada geração até validar estado jogável completo; retenção conserva quatro backups recuperáveis, poda valida backups em uma única thread e só trava durante exclusão; `load_world` trava enumeração+reconstrução para impedir corrida com a poda. `save_edit_revision` ignora worldgen/streaming/iluminação e aumenta após mutações efetivas de blocos/fluidos. Referências completas, ressalvas e SHAs no handoff 32–36 arquivado.

**Etapa 35, cache CI validado:** `7a556186943ef777a4d79fd5872a64a999ec697b` criou cache de `Cargo.lock` antes de `Swatinem/rust-cache`; `d5772dcc90b48eacd850a7b5a110c7ba6cae75db`, https://github.com/sarakborges/mineclone/actions/runs/35224192069, confirmou HIT exato no lock e artefatos (~412 MB), geração de lock pulada, Clippy 15,56s vs ~3m35s em run anterior. `Cargo.lock` permanece gitignored por decisão existente; índice crates.io ainda pode atualizar. Sem `cargo test`.

## Etapa 37 — data recuperável na lista de mundos e limite de snapshot [CÓDIGO + CI VERDE; QA PENDENTE]

- `ec6eb38542605c7dcc44d03bb121b564e250cc57`: `src/world/save_catalog.rs` expõe `PruneRegistries` próprio e `list_verified_worlds`, que enumera candidatos e, **somente quando chamado em worker**, tenta gerações em ordem decrescente usando `load_snapshot` + `validate_playable`; usa `SAVE_LOCK` enquanto abre/reconstrói cada mundo para impedir a poda de remover o candidato. Só exibe mundos com ao menos uma geração recuperável; timestamp é o da geração efetivamente validada, não de manifesto corrompido. Também rejeita na escrita JSON de snapshot acima de `MAX_SNAPSHOT_BYTES = 512 MiB` antes da publicação; um save não pode confirmar arquivo que seu próprio loader rejeitaria por tamanho.
- `ff3918de1c3bff5d7acb842ebdaee8f3dfe71bd0`, CI https://github.com/sarakborges/mineclone/actions/runs/35227081508 success Clippy rigoroso + cargo check: `src/screens/world_selection.rs` inicializa uma única tarefa `asteria-world-scan` reutilizável se a tela for reaberta durante varredura; copia registries do conteúdo, verifica snapshots fora do frame e entrega resultado por `Arc<Mutex<Option<Result<...>>>>`, consultado via `try_lock`. Enquanto analisa mostra estado de verificação; após retorno cria botões e mostra timestamps verificados. Erros de iniciar/pânico capturado são exibidos; Load não avança antes de acabar a análise. O carregamento continua revalidando no clique, pois arquivos podem mudar após a varredura.

**Limites explícitos:** o worker ainda reconstitui um mundo completo por candidato e pode usar muita CPU/RAM em mapas grandes; a verificação ocupa `SAVE_LOCK` de cada mundo até concluir, podendo atrasar uma gravação simultânea. Não existe medição de tempo nem prova em Windows. Strings novas na UI estavam hardcoded nesta etapa e foram integradas à localização na etapa 41. Se todos os backups de um mundo falharem, ele fica fora da lista e há warning em log; não há UI de reparo. O snapshot novo ainda é capturado e gravado via `fsync` na thread principal; serialização integral em memória foi substituída na etapa 39. CI não executou jogo nem testes de corrupção. `VERSION` na etapa 37 era `0.20.3`.

## Etapa 38 — evitar duplicação e instrumentar o save [CÓDIGO + CI VERDE; MÉTRICAS WINDOWS PENDENTES]

`e37dc2b93308f75908e7c43ea54950e6de6e4b1c`, CI https://github.com/sarakborges/mineclone/actions/runs/35230616089 success Clippy rigoroso + Check: `src/world/save_session.rs::persist` captura uma vez o estado persistido de posição/inventário/jogador e deriva a assinatura de baseline a partir do snapshot exato gravado, em vez de chamar `saved_state()` separadamente antes de capturar. Usa `Instant` e log `World <id> saved: capture=..., publication=...` somente após publicação bem-sucedida, distinguindo captura/serialização de chunks da publicação de JSON, fsync e dispatch de limpeza. Não inventar durações sem logs de execução real; o `world_revision` é lido após captura do mesmo recurso imutavelmente emprestado. A detecção periódica de alterações por `saved_state()` continua normal.

## Etapa 39 — streaming JSON com limite durante a escrita [CÓDIGO + CI VERDE; QA PENDENTE]

`c0c001cec61b09e3c875347bf7aa69c760c1b32a`, CI https://github.com/sarakborges/mineclone/actions/runs/35232620292 success (Clippy rigoroso e cargo check, cache Cargo.lock hit e resolução pulada): `src/world/save_catalog.rs::publish_json` deixou de construir `serde_json::to_vec` + newline de até 512 MiB em RAM. Agora usa `serde_json::to_writer` em `io::BufWriter` e `SnapshotSizeLimit<W: Write>` que desconta todos os bytes, inclusive newline, e recusa acima de 512 MiB antes do rename. Só após serializar com sucesso realiza `flush`, `file.sync_all` e `fs::rename` do arquivo temporário; qualquer erro remove o temporário e não publica o manifesto. Manifestos usam mesmo buffered writer, sem limite de snapshot. Formato JSON sem alterações intencionais. O escritor permanece síncrono, e a captura ainda aloca representações `DiskChunk`; a otimização visa menor pico de memória, NÃO prova melhor latência ou roundtrip.

## Etapa 40 — leitura JSON buffered [CÓDIGO + CI VERDE; QA PENDENTE]

`eda91b78c0526f17eb6d7a597c39752064e5383d`, https://github.com/sarakborges/mineclone/actions/runs/35233747753 success (Clippy rigoroso e cargo check): em `src/world/save_catalog.rs`, `read_json` usa `serde_json::from_reader(io::BufReader::new(fs::File::open(path)?))` em vez de `serde_json::from_slice(&fs::read(path)?)`. Todos os consumidores, inclusive `load_snapshot`, manifestos e prune, seguem o mesmo parser JSON sem vetor adicional de bytes de arquivo. Validação prévia de arquivo regular e máximo de 512 MiB do snapshot, fallback e lock permanecem. Possível redução de pico de RAM, NÃO medida no Windows; leitura/decodificação de mundo no clique ainda bloqueia a UI.

## Etapa 41 — localizar feedback da seleção [CÓDIGO + CI VERDE; QA PENDENTE]

`410de634137803eecaf847b7a9aa77c2577aacc8` adicionou chaves de estado/erro/seleção/carregamento em `data/localization/english.json`; `c2bdbca1aca35d40b5da804dd2466bd3378cfd2b` trocou mensagens hardcoded na tela por `UiLocalization` e `ActiveLanguage`, porém CI https://github.com/sarakborges/mineclone/actions/runs/35234230147 detectou Clippy `too_many_arguments` (8/7) em `refresh_world_list`. `eba1a7a2d7120f143ffec2577396d6091dc81de4`, CI https://github.com/sarakborges/mineclone/actions/runs/35234439682 completed/success: introduziu `WorldSelectionScanContent` como `SystemParam` coeso e somente leitura para os cinco registries necessários à verificação; Clippy `-D warnings` e cargo check verdes, sem supressão. Projeto cadastra somente idioma inglês atualmente; isto integra textos à infraestrutura, não cria uma tradução portuguesa. O scanner mantém sua semântica e seus limites anteriores.

## Etapa 42 — UI contextual e sidebar lateral [FEATURE BRANCH; CI VERDE NO CÓDIGO; QA VISUAL PENDENTE]

Pedido do usuário (17/09): sidebar das telas de opções/criação colada à borda esquerda da viewport e conteúdo restante adaptativo; pausa separada em seções WORLD/GAME; configurações gerais exclusivamente Graphics/Languages/HUD e opções específicas do mundo exclusivamente World Settings/Game Rules acessíveis pelo pause. **Cards sem bordas**; botões secundários e controles podem ter contorno. Não inventar bordas em cards no polimento posterior. A criação de mundo continua com seções General/Game Rules e controles de world mode, por definição necessários ao mundo novo.

- Branch `feat/ui-redesign-sidebar-pause` criada de `develop` em `cfa934acf0cf34627e995c99b977ae6a0323737c`; `develop` avançou 1 commit independente de cores dyed JSON (`1504f47c77016e21650c4bfae514a0a82b12a780`) durante o trabalho. Draft PR #11: https://github.com/sarakborges/mineclone/pull/11, base `develop`, não efetuar merge sem revisão e QA.
- `502cbe569665b3f1ecf825e22913777a48556ded` introduziu `SettingsScope::{Game,World}` em `src/app/settings_state.rs`; `20fc212f67f54993ac3a1fa406967b5338d24a19` inicializou recurso em `SettingsScreenPlugin`.
- `b8331733b5d5dfbce029b4682dc17158903aa574` adicionou grupos e botões distintos ao pause, troca do scope antes da transição, sem tocar na semântica de persistir ao Leave/Exit e manter o mundo após erro. `f0b92c11f4209bee055c284f1fc697e1417f554e` separou contextos CreateWorld, Start, InWorldGame e InWorldWorld, removendo opções do mundo de Settings gerais; sidebar agora começa em x=0 (abaixo do cabeçalho), largura 280 px com máximo 35% da tela, conteúdo `flex_grow` e `min_width=0` ocupa restante sem cap de 1120 px; scroll individual preservado.
- `84e8df0bbc3bbd78887a237f341d631575fb37b3` adicionou strings inglesas dos novos grupos/contextos em `data/localization/english.json`. `938ef08f52fea24b4d4ab4b33f16f194db19b8b4` fez `surface::hud_container` zerar a borda de painéis, preservando contornos funcionais de inputs, categorias e slots; superfícies/sombras permanecem. `11831d7b13538ba2b973ee284964fd6648651dff` elevou `VERSION` a `0.21.0` por feature minor; `Cargo.toml` não sincronizado.
- CI do commit `938ef08` em https://github.com/sarakborges/mineclone/actions/runs/35238055766: **sucesso**, Clippy `--locked --all-targets --all-features -- -D warnings` e `cargo check --locked`; hit do cache de Cargo.lock e de crates. Esse run precede somente o bump de VERSION e o texto deste handoff, não comprova execução visual nem QA Windows. O PR permanece draft.

**Pendências UI:** avaliar comportamento real em 720p/1080p/1440p, fluxo abrir/voltar World Options e Game Options, resize, clipping dos controles/rodapé da criação, legibilidade e hover/foco; o rodapé da criação ainda usa dois botões fixos de 470 px e pode estourar viewport estreita. Melhorar componentes secundários, seleção de mundos e HUD numa fase seguinte, sempre cards sem bordas; preservar atualizações por change detection. Sem `cargo test` não autorizado. QA dos saves anterior continua independente e pendente.

## Próximos passos — prioridade

1. Validar visualmente UI em Windows, especialmente 720p e rodapé da criação, sidebar flush e espaçamento, reabrir os dois escopos após retornar à pausa e confirmar ausência de opções do mundo no menu geral; corrigir clipping antes do merge.
2. Medir no Windows usando logs `capture`/`publication` em mundos pequenos/grandes e com chunks arquivados; reduzir próximo gargalo empiricamente, preservando contrato Leave World/Exit: somente sair após gravação confirmada; erro mantém mundo carregado. Avaliar fsync, captura, cópias de registry e serialização em separado.
3. Revisar orçamento CPU/RAM e retenção de lock do scanner assíncrono da etapa 37; a localização inglesa foi corrigida na etapa 41. Índice é retrato temporal, `load_world` precisa revalidar no clique; examinar abertura de mundo sem frame longo, com preservação de erros e estado.
4. QA real Windows/restart: nome duplicado e mundos isolados, blocos/fluidos/brush em chunks arquivados, inventário, posição, regras, relógio, autosave/Leave/Exit e reabertura do processo; corromper geração mais recente e confirmar data de fallback; interrupção entre snapshot/manifesto, permissões, recuperação da poda, carga de mundos grandes. Sem alegar sucesso sem executar.
5. Preservar cache CI de lock e artefatos, `--locked` e Clippy `-D warnings`; não introduzir `cargo test` sem pedido. Não sincronizar `Cargo.toml` sem decisão expressa.
