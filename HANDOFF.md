# HANDOFF — Asteria / Mineclone

Repositório `sarakborges/mineclone`, branch **`develop`**, Rust + Bevy 0.19.1. **Fonte canônica persistente: atualizar na mesma sessão de toda alteração material.** Histórico integral sem perda: [.44](https://github.com/sarakborges/mineclone/blob/902e3b893883a047f6469552ee7898b553280f6f/HANDOFF.md), [.43](https://github.com/sarakborges/mineclone/blob/d9e4b3b75e44d4a05ce05cfbf45e2f375d4aa1b2/HANDOFF.md), [.42](https://github.com/sarakborges/mineclone/blob/9718eb0aa0fcc476b33e8bea8f82c0ad2b2ad8b4/HANDOFF.md), [.41](https://github.com/sarakborges/mineclone/blob/479d9d09605e58584a528222969f575d145f4c4e/HANDOFF.md), [.40](https://github.com/sarakborges/mineclone/blob/fc8d6d154d81588e9362baa33e09ad4562617d2b/HANDOFF.md), [.39](https://github.com/sarakborges/mineclone/blob/31b05c55b9d5cbda95cd6bfe4ab268e269425f6b/HANDOFF.md), [.38](https://github.com/sarakborges/mineclone/blob/9ff4279337b6ba2af8d03274ee0d9be8918b75a3/HANDOFF.md), [.37](https://github.com/sarakborges/mineclone/blob/1b90522fe0bde385bfa48c09fc93a00de54b0488/HANDOFF.md), [.36](https://github.com/sarakborges/mineclone/blob/154e4bfd0d050cb78a7e74c9c210b3aa9676c167/HANDOFF.md), [.35](https://github.com/sarakborges/mineclone/blob/355c0f5eeae8d08738c49f7a895c2d9534714793/HANDOFF.md), [.34](https://github.com/sarakborges/mineclone/blob/814a59b86fe32c73655a7d27ee4142b3949bf0ae/HANDOFF.md), [.33](https://github.com/sarakborges/mineclone/blob/a90e42a61d18a8aa4e326b17450f80e5b334de53/HANDOFF.md), [.32](https://github.com/sarakborges/mineclone/blob/5900057c6e7ec8d53684bf877a47c499ae6d3b5b/HANDOFF.md), [.31](https://github.com/sarakborges/mineclone/blob/6b8051628aa49c3b01ffca782144b8fc84705365/HANDOFF.md), [pré-.31](https://github.com/sarakborges/mineclone/blob/72b60b5a4e70a9960b3108debdf77d7a8510c4b1/HANDOFF.md). Compactação NÃO encerra bugs.

## Regras obrigatórias

`continua` / `go` = **investigar e alterar código efetivamente**, não só conferir CI/planejar/encerrar sem feedback. Consultar HEAD/VERSION/código/CI antes de escrever, commits direto `develop`, sem branches não solicitadas. Cada bloco código/dados jogáveis incrementa `VERSION` SemVer: PATCH fix/refactor compatível, MINOR feature, MAJOR quebra; docs isoladas não incrementam. `Cargo.toml` versão 0.10.16 é deliberadamente independente; não modificar ao incrementar VERSION. HANDOFF NA MESMA SESSÃO com versão, commits, CI observada, bugs abertos e próxima etapa. Corrigir erros/warnings reais sem silenciar. CI `cargo clippy --all-targets --all-features -- -D warnings` + `cargo check` apenas; **não executa testes unitários, gameplay, benchmark nem fmt**. Não afirmar correção visual sem feedback/execução. Não insistir em disclaimers cargo local ou esperar cargo run, não criar imagens não solicitadas, preservar assets originais. Comunicar achados e entregas úteis, sem prometer trabalho assíncrono.

## Arquitetura / contratos

`ARCHITECTURE.md`: owner único por fato, SystemParams de domínio, UI `src/ui`, `TargetedBlock` único, trabalho change-driven, filas deduplicadas `DeduplicatedQueue<T>`/`VoxelUpdateQueue` e `FrameWorkBudget`, evitar scans globais/dirty writes redundantes. Streaming visible/preload → geração async → integração + seed de luz imediato uma vez por residência (.38) → snapshot halo 3×3×3 → mesh async → render → archive/unload. Visible supera preload, preempção de não crítico, output stale reencaminhado sem starvation. Terreno/fluidos/luz malhas/filas independentes; revisões `ChunkRemeshTasks::lighting_revisions` ≠ `VoxelWorld::chunk_mesh_revisions`; halo pós-mesh inicial .33, unload notifica 26 vizinhos geometry/fluid .40, relax interativa publica por lote .41. Render HDR world order0 Skip, viewmodel order1 tonemap, UI SDR order2. Céu por bioma/relógio exato; fogColor artístico pendente. Sol/lua órbitas/rise-set por dimensão, nuvens world-space. Hidrologia endpoints XYZ compartilhados, margens gerais .12, fontes naturais de água autoritativas fora solver, jamais criar água suspensa. Held block observa hotbar, R só placement.

## Estado em 16/09/2026

**VERSION = 0.15.45.** Implementação `.45`: `src/world/hydrology/region/water.rs` commit `8c9df7f0a21a57b9bdadd57dfb7c12757ae5886f`; `src/world/density_sampling/hydrology.rs` commit `71d6969032bf951d8cc864d2487facad1017488e`; `src/world/hydrology/region/density.rs` commit `398a5ca520759cd85016e9bd1073ecb083b86f9a`; bump `VERSION` commit `96acecc45098948686a8eecad80ddc8e843a0788`. HEAD final = commit deste HANDOFF; consultar develop. **CI `.44` CONFIRMADA SUCCESS:** push `35148263437`, job `104969742569`, Clippy e Check SUCCESS; `.43` push `35147277012`, `.42` `35146865609`, `.41` `35144120702`, `.40` `35143134455`, `.39` `35142554264`, `.38` `35141651000`, `.37` `35140172992` Clippy/Check success. **CI `.45` disparada após bump: verificar novo run push/PR; não afirmar aprovação antecipada.** Testes unitários escritos em código mas NÃO executados pelo workflow; não há gameplay ou FPS medidos.

**.45 — vetar escavação de água sem leito e headroom de rio seco, correções fundamentadas no código, gameplay NÃO confirmado:** `.43/.44` alinhavam seleção de água física e `enforce_hydrology_water_volume`, MAS `HydrologyRegion::density_column_profile` ainda aplicava `river` e `water_bodies` negativos em TODAS as amostras gráficas, mesmo onde `supported_water_at` rejeitava a mesma fonte por `surface_height + 0.5 < bed_level`. Além disso `sample_density_column_hydrology` ainda chamava `river_surface_at` não filtrado para `headroom` de rio sem água. Isso gerava leitos/cavidades sem fluido mesmo após `.44`. `.45` centraliza teste `bed_has_support(Option<f32>,bed_level)` em `region/water.rs`, compartilha com `choose_water`, acrescenta `supported_river_surface_at`, usa-o ao gerar colunas com altura real e mantém API `river_surface_at` genérica sem filtro. No perfil de densidade, rios dentro da pegada molhada e lagos com strength>0 são ignorados quando leito não encontra terreno de suporte; lago exterior seco preserva sua graduação de margem, e rio fora do perfil molhado mantém margem externa. Sem altura original (API genérica) comportamento anterior preservado. Não alterou altura da água, força do carving suportado, perfil shore global, fluido físico ou solver. Testes unitários novos em `region/density.rs`: `unsupported_lake_does_not_carve_a_dry_basin` (lago água100/leito94, terreno85 sem carve vs terreno95 com carve); `unsupported_river_does_not_carve_a_dry_channel_or_headroom` (rio água100/leito93, terreno80 rejeita, 94 aceita), mais `support_rule_accepts_exact_bed_boundary` em water.rs. **Testes adicionados, não executados.** Riscos restantes: ocean_delta ainda tem seleção independente; grafo pode selecionar aresta dominante não suportada sobre outra viável, e margens exteriores podem mudar relevo seco. Investigar por seed/coords antes de encerrar P0 #2/#4.

**.44 — seleção de água suportada também para densidade da coluna:** `src/world/density_sampling/hydrology.rs` `c2cf2a0b4809613a5972e3228678befe6e133a1a`, `generation/density.rs` `5cf2d55eccf22138c55fc8c19528491c2e390e0b`, `density_sampling.rs` `f590969d1ebc3db53b53ae38fa1918da428faadc` e ajuste teste `d31513dd10681617f5b119d53ab9311445892611`, bump `7bb8825b26812ccbc445153c8d57ba55f20a8ce4`, CI SUCCESS. Geração passa altura original ao `sample_density_column_hydrology`, `supported_water_at` define wet volume, uso genérico None preserva water_at antigo. Teste da água de rio 83/leito78 abre y80, não cava lago impossível y95 escrito e não rodado. `.45` cobre carving regional/headroom que ainda divergia.

**.43 — fallback de candidato físico:** `region/water.rs` `e523b3442b29efc8312c0af2dac4a8079cc9c068`, `generation/fluids.rs` `e0bc06891e32297d359fba65f6532628c3550fd7`, bump `cc11d3b8835eface1d6bd265fc454cf33ae47233`, CI SUCCESS. `supported_water_at` filtra individualmente lago/rios/oceano antes de escolher nível; `water_at`/`water_near` gerais continuam iguais. Guarda da água suspensa preservada. Testes escritos não rodados.

**.42 — saída oceânica MOLHADA priorizada:** `drainage.rs` `9c51edf5852b7b91985e1ce648be86edc1341021`, teste borrow `bf9e954cb39674732609e479fb1e33c8ce55f204`, bump `1afb51a25ee7655df1fd2e6aa3c2b864e1164b9f`, CI SUCCESS. Ordem oceano radius1→oceano radius4→descida local→escape. Dry fringe rejeitado; arestas radius4 potencialmente ~512 blocos: verificar região/FPS. Teste escrito não rodado.

**.41** luz interativa remesh a cada lote (`propagation.rs`), CI success, gameplay não confirmado; **.40** invalidação halo unload 26 vizinhos geometry/fluid, CI success; **.39** céu provisório Top para halo superior ausente, CI success; **.38** seed direta de luz na integração, CI success. **.37** culling real radius*2.5 de margens; **.36** perfil de leito/água/carve comum; **.35** mountain belt estreito; `.34` Clippy, `.33` halo initial catchup, `.32` lua fase Night, `.31` diagonais luz. Detalhes integrais nos HANDOFFs históricos. **ÚNICO encerramento confirmado pelo usuário neste ciclo: céu por bioma `.24`.**

## Nove defeitos reportados em 16/09 — TODOS ABERTOS até confirmação

| # | Prio | Sintoma/critério |
|---|---|---|
| 1 | P0 | Chunks inteiros pretos/costuras luz/AO; `.33/.38/.39/.40/.41` atacam causas delimitadas, não confirmação. Diferenciar persistente/transitório e medir FPS. |
| 2 | P0 | Rios terminam no nada: `.42` foz oceânica, `.43/.44/.45` alinham fontes físicas e escavação; ainda provar grafo→water_at→density→fluidos end-to-end entre regiões e bacias remotas por seed/coords. |
| 3 | P0 | Depois de 2000 blocos só Plains/Mountains; `.35` belt estreito não confirmado; medir `BiomeField::sample_surface().primary_id` e aspecto visual. |
| 4 | P0 | Margem em descidas afunda antes da água: `.36/.37/.43/.44/.45` parciais; verificar suporte, manter margem `.12`, sem água flutuante. |
| 5 | P1 | Rio–lago junção abrupta, suavizar XY/largura/profundidade/altura compartilhando endpoints; `.43–.45` não são suavização visual. |
| 6 | P1 | Túneis paredes retas em aberturas na superfície e cruzamento rio–túnel. |
| 7 | P1 | Lua parece seguir câmera (`celestial.rs` usa camera_position + offset), analisar referencial sem mudar órbita às cegas. |
| 8 | P1 | Lua visível durante dia, `.32` setPhase Night ainda sem confirmação. |
| 9 | P1 | Target highlight não cobre todas camadas de textura, verificar overlays/bounds/alpha. |

Outros: fogColor artístico; FPS baixo andando sob streaming sem benchmark confiável; R/held `.18` sem gameplay. HUD planejado Player inferior esquerdo avatar placeholder `Yogg'Sara`, vida `50 / 100` dentro barra, status acima; Target acima crosshair, ícone esquerdo com slot do inventory, texto direito sombra tooltip. Históricos confirmados pelo usuário: céu `.24`, margens gerais `.12`, hotbar vazia, ghost alpha uniforme, dye, HUD/fog anteriores; não regredir.

## Próxima execução concreta

1. Conferir CI `.45` (`VERSION` commit `96acecc...` ou PR HEAD) e HEAD; corrigir erro/warning real com bump e handoff. Unit tests não rodam no workflow.
2. P0 rios: por seed/coords medir real `.42` arestas oceano radius4 vs graph/água física em duas regiões, e ocean_delta vs ocean water_at/suporte; observar margens externas e confluências `.45`. Evitar mudar níveis/margem global às cegas. Teste end-to-end determinístico preferível a mais hipótese.
3. P0 chunks pretos: auditar snapshot inicial content vs luz, `chunk_remesh` filas/prioridade/starvation; medir custo `.38` seed, `.40` remesh e `.41` luz sob FPS em movimento. `enqueue_lighting_change` enfileira seis cardinais até quando não possuem conteúdo na face voltada; filtrar contadores O(1) só se comprovada segurança e teste.
4. P0 diversidade: amostra trajetória 2000 blocos por primary_id e visual, depois P1 túneis/Lua/target. Cada bloco código atualiza VERSION e HANDOFF na mesma sessão; jamais fechar os nove sem confirmação do jogo.
