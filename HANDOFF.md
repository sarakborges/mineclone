# HANDOFF — Asteria / Mineclone

**Fonte canônica:** `sarakborges/mineclone`, branch `develop`; Rust + Bevy 0.19.1. **Versão raiz `VERSION`: `0.20.3`**: bloco de save aberto, não subir sem QA real. `Cargo.toml` `0.10.16` tem versionamento independente. **Último código verde:** `8860a1139c59ddc4af997864371384640566bb63`, CI https://github.com/sarakborges/mineclone/actions/runs/35256477568: auditoria dos três idiomas, Clippy `--all-targets --all-features -- -D warnings` e `cargo check --locked` concluídos com sucesso. CI não comprova execução/roundtrip no Windows.

## História integral obrigatória (não descartar requisitos)

- [Etapas 0–12](docs/handoffs/auditoria-0-12-2026-09-17.md), blob `34161a1ec793041a738ec704e41dc345cc87fff6`.
- [Etapas 13–16](docs/handoffs/auditoria-13-16-2026-09-17.md), blob `cbbb6710e8be6b132467c943f03ed8df326509e3`.
- [Etapas 17–20](docs/handoffs/auditoria-17-20-2026-09-17.md), blob `bc4a5e7f5ff723cc6f52ef0c50edce73b37b5da2`.
- [Etapas 21–24](docs/handoffs/auditoria-21-24-2026-09-17.md), blob `8b35a7c3680f7df1d3aaf4e142fcf39ed605036d`.
- [Etapas 25–27 e requisitos de save](docs/handoffs/auditoria-25-27-2026-09-17.md), blob `681c00fbb1674687affd4cf87d06c9317213c707`.
- [Etapas 28–31 na íntegra](docs/handoffs/auditoria-28-31-2026-09-17.md), blob `9b46ae054ed205590d67a353fb5117c471e419a8`.
- [Etapas 32–36 na íntegra](docs/handoffs/auditoria-32-36-2026-09-17.md), blob `33ea041165e699a5a95480f60f9b84a26d33d005`.
- [Etapas 37–44 e handoff anterior integral](docs/handoffs/auditoria-37-44-2026-09-17.md), blob `7a097aac02ebbcad2b5cadf0a2a79dc3cf191b67`, arquivado sem cortes em `eb4d0346aed06db3110b704a57162726a45d3940`. Inclui SHAs, resultados CI, detalhes de scanner, serialização e trava. Correção posterior: prioridade de Back resolvida em `f06c68c`. Ler também `ARCHITECTURE.md`.

**Regras permanentes:** implementar commits coerentes na `develop`, atualizar este handoff a cada checkpoint (SHA, CI, limitações e próximos passos) e arquivar versão integral antes de condensar. Incrementar semanticamente `VERSION` após cada bloco funcional concluído, não por documentação isolada, e não fechar saves sem QA. Corrigir todos os warnings sem supressão; CI exige auditoria de PT-BR, espanhol e inglês, Clippy rigoroso e `cargo check --locked`. Não executar/adicionar `cargo test` sem autorização expressa. Não alterar PNG/GLB autorais. Pause NÃO congela o mundo, fluidos ou horário. `Cargo.lock` é gitignored, CI cacheia lock e compilados: preservar otimização. Não inventar execuções ou números de desempenho. Informar progresso concreto, sem prometer trabalho em segundo plano.

## Estado consolidado até etapa 49

UI de nome único e mundos isolados, manifesto/snapshot imutáveis com commit marcador, chunks alterados residentes/arquivados, IDs portáveis, inventário, posição, regras, dimensão e relógio; autosave após 60 segundos apenas quando assinatura de mundo/posição/inventário/regras mudou; Leave/Exit salvam antes de sair e permanecem no jogo se falhar. Fallback valida geração inteira inclusive inventário/relógio/chunks; retenção considera quatro gerações realmente recuperáveis. Prune fora do frame, catálogo verificado fora do frame com data do snapshot recuperável, carga selecionada em worker. Registro de duração de capture/publication em `save_session.rs`; JSON buffered com limite 512 MiB e leitura buffered. Travas por mundo desde etapa 43; etapa 45 substituiu abertura simultânea de todos os candidatos (etapa 44) por lease e um snapshot por vez; etapa 46 retoma poda após leitura sem novo save. Etapa 47 descarta resultado abandonado fora do frame e sem reter `VoxelWorld` grande fora do menu; etapas 48/49 reduzem alocação por bloco e fecham variantes de nomes reservados do Windows. Histórico e ressalvas originais no arquivo 37–44. **Protocolo de QA ainda NÃO EXECUTADO:** [docs/save-roundtrip-qa.md](docs/save-roundtrip-qa.md).

## Etapa 45 — cancelamento prioritário e leitores de snapshots com limite [CÓDIGO + CI VERDE; QA WINDOWS PENDENTE]

- `f06c68c39563b07095f383b060639463b0aac659`, CI https://github.com/sarakborges/mineclone/actions/runs/35251176602 success: `src/screens/world_selection.rs` ordena `handle_world_selection` antes de `poll_world_load` e pré-varre Back antes de Select/Load; Back ganha mesmo com conclusão simultânea. A marcação de abandono foi refinada na etapa 47. Interação não testada em Windows.
- `f55f258429769bb072de571ee966f4159b9a0583`, CI https://github.com/sarakborges/mineclone/actions/runs/35252851074 success: `save_catalog.rs` substitui abertura simultânea de arquivos do fallback por `SnapshotCandidates` com `ReadLease` RAII/contador atômico por mundo; decodifica sem mutex, abrindo um snapshot por vez. Poda só exclui sem leitores. `WorldGate.prune_running` por mundo impede que cleanup de A impeça iniciar o de B. Formato inalterado. Adiamento até próximo save resolvido na etapa 46. Workers ainda decodificam mundos completos com custo CPU/RAM; gates pequenos persistem até fim do processo. Não houve QA de corrida/Windows nem cargo test.

## Etapa 46 — limpeza retoma quando último leitor termina [CÓDIGO + CI VERDE; QA WINDOWS PENDENTE]

- `e01044b54c1843b811c164c984e72b4e7de2f07c`, https://github.com/sarakborges/mineclone/actions/runs/35254967476 success: `WorldGate` ganhou `reader_signal: Mutex<()>` e `readers_finished: Condvar`; `ReadLease::drop` sincroniza decremento do contador e notifica último leitor. Podador valida quatro gerações fora da trava, adquire `write`; se existem leitores, libera `write`, aguarda condição sem busy-spin, readquire lock e revalida contador antes de excluir manifests e snapshots. Não depende de outro save nem ocupa trava do writer durante espera. Idiomas, Clippy rigoroso e check passaram, sem cargo test.
- `18a4cc8d7a50939d9513f54cdc539c8c2d985b26` adicionou [protocolo manual QA Windows e roundtrip](docs/save-roundtrip-qa.md): nomes, chunks arquivados, fluidos/brush, relógio, autosave, falha de escrita, corrupção/fallback, publicação interrompida, navegação e poda. Documento não prova execução.
- Limites: podador pode aguardar indefinidamente se leitor travar; não há timeout/cancelamento. Múltiplos mundos podem iniciar simultaneamente workers pesados; falta orçamento/concorrência global. Snapshot capture, JSON e fsync ainda síncronos; nenhuma latência medida no Windows. Teste real de Condvar/ReadLease, reinício e recuperação pendente.

## Etapa 47 — descarte de load cancelado fora do frame [CÓDIGO + CI VERDE; QA WINDOWS PENDENTE]

- `bb667e634f1f819748350cbccd1f6b8fd4876800`: `WorldLoadResult` usa `WorldLoadSlot { abandoned, complete, result }` num `Mutex`, sincronizando cancelamento com publicação. Back e OnExit chamam `PendingWorldLoad::abandon`: marcam abandono e movem resultado pronto para thread de descarte, evitando liberar `VoxelWorld` grande no frame. Resultado concluído após cancelamento é destruído pelo próprio worker, não armazenado no recurso da tela. `poll_world_load` aguarda `complete`, descarta abandonados e só aplica mundo válido no sistema Bevy. Worker fica rastreado até finalizar; falha ao criar thread de descarte produz warning e pode destruir closure na thread chamadora. QA de UX/memória pendente.
- Primeira CI https://github.com/sarakborges/mineclone/actions/runs/35255555252 falhou `unused_mut`/`collapsible_if`; `709c14c881734d453a1cef51069f3a559b8a1a3c` corrigiu sem supressão. CI final https://github.com/sarakborges/mineclone/actions/runs/35255733070 success idiomas, Clippy -D warnings, cargo check; sem cargo test.
- `SelectionError` caption inicial começa vazio (alteração colateral do commit Clippy), sendo preenchido por `sync_world_selection_feedback` em Update quando o estado muda. Avaliar regressão em erro inicial de spawn; restauração direta de `state.error.clone()` é preferível se houver atraso/flicker. Não alegar desempenho ou cancelamento aferido.

## Etapa 48 — decodificação de propriedades sem HashSet por bloco [CÓDIGO + CI VERDE; QA PENDENTE]

`68af3a88dcd8e34dd7259da3c05ec6b03336289e`, CI https://github.com/sarakborges/mineclone/actions/runs/35256258080 completed/success (idiomas, Clippy rigoroso, cargo check): em `src/voxel/chunk_disk.rs::DiskChunk::into_chunk`, substituído `HashSet` alocado para cada bloco por verificação dos itens anteriores na própria lista de no máximo oito propriedades; campos vazios, duplicados e limite seguem rejeitados. Validação agora ocorre integralmente antes de internar propriedades do bloco. `GitHub.fetch_commit` confirmou diff estrito ao caminho descrito. Sem benchmark ou teste de corrupção/roundtrip: redução de alocação estrutural, não ganho temporal medido.

## Etapa 49 — nomes reservados especiais no Windows [CÓDIGO + CI VERDE; QA PENDENTE]

`8860a1139c59ddc4af997864371384640566bb63`, CI https://github.com/sarakborges/mineclone/actions/runs/35256477568 completed/success (idiomas, Clippy -D warnings, check): `src/world/world_names.rs::validate_world_name` agora barra também os nomes de dispositivo `COM¹`, `COM²`, `COM³`, `LPT¹`, `LPT²`, `LPT³` (com ou sem extensão), além dos nomes ASCII que já eram barrados. Regra confirmada na documentação Microsoft Learn Win32, https://learn.microsoft.com/pt-br/windows/win32/fileio/naming-a-file. Mensagem de erro esclarece que o limite é de 200 unidades de código UTF-16, não necessariamente 200 caracteres Unicode. Nenhum teste Windows executado.

## Próximas etapas, em ordem

1. Avaliar por inspeção/QA orçamento de concorrência/CPU/RAM de scan, load e prune entre mundos, sem sacrificar fallback nem bloquear frame. O descarte do resultado cancelado foi resolvido, mas decodificação IO não é cancelada precocemente; avaliar cancelamento cooperativo de fallbacks. Podador ainda pode aguardar lease sem timeout. Evitar limiar arbitrário ou benchmark inventado.
2. Executar QA Windows real do [protocolo](docs/save-roundtrip-qa.md): `capture`/`publication`, tempos de scan/load, RAM/CPU, leitor+poda, dois mundos, Back simultâneo, corrupção e transição. Acrescentar criação com `COM¹`, `LPT²`, variantes com extensão e nomes Unicode até 200 unidades UTF-16. Nenhuma medição foi feita pelo assistente.
3. Roundtrip e restart: nomes repetidos, blocos/fluidos/brush arquivados, inventário, posição, regras/relógio, autosave, Leave/Exit, fallback com timestamp, publicação interrompida, permissões e poda. Registrar PASS/FAIL/NOT RUN sem supor resultado.
4. Com dados reais, otimizar gargalo medido sem alterar contrato Leave/Exit: só sair após publicação confirmada, erro mantém mundo. Gravação ainda captura/escreve/`fsync` no frame.
5. Preservar CI/cache (`--locked`, `-D warnings`), não executar/adicionar `cargo test` sem autorização; subir `VERSION` semanticamente só após QA/fechamento funcional. `Cargo.toml` permanece independente.
